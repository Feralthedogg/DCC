<!-- Generated by tools/generate_api_reference.py; do not edit. -->

# `<dcc/bot/listeners.h>`

Source: [`include/dcc/bot/listeners.h`](https://github.com/Feralthedogg/DCC/blob/v2.0.0/include/dcc/bot/listeners.h)

## `DCC_LISTENER_CONFIG`

Initializes an empty borrowed listener configuration.

```c
#define DCC_LISTENER_CONFIG_INIT \ {sizeof(dcc_listener_config_t), DCC_LISTENER_CONFIG_VERSION, NULL, NULL, \ NULL, NULL, 0U, NULL, NULL, NULL, 0U} static inline dcc_listener_config_t DCC_LISTENER_CONFIG(void) { dcc_listener_config_t value = DCC_LISTENER_CONFIG_INIT; return value; }
```

## `DCC_LISTENER_CONFIG_INIT`

Public macro `DCC_LISTENER_CONFIG_INIT`; arguments follow the owner header contract.

```c
#define DCC_LISTENER_CONFIG_INIT ...
```

## `DCC_LISTENER_CONFIG_WITH_POLICY`

Returns an empty configuration borrowing policy through registration.

```c
static inline dcc_listener_config_t DCC_LISTENER_CONFIG_WITH_POLICY( const dcc_listener_route_policy_t *policy ) { dcc_listener_config_t value = DCC_LISTENER_CONFIG(); value.policy = policy; return value; }
```

## `DCC_LISTENER_DAILY_KST`

Returns a versioned daily Korea-time schedule value.

```c
static inline dcc_listener_schedule_target_t DCC_LISTENER_DAILY_KST( uint8_t hour, uint8_t minute ) { dcc_listener_schedule_target_t value = { sizeof(value), DCC_LISTENER_TARGET_VERSION, DCC_LISTENER_SCHEDULE_DAILY_KST, 0U, hour, minute}; return value; }
```

## `DCC_LISTENER_EVENT_HANDLER`

Wraps a generic event handler without casting it.

```c
static inline dcc_listener_handler_t DCC_LISTENER_EVENT_HANDLER( dcc_app_event_fn handler ) { dcc_listener_handler_t value = {0}; value.event = handler; return value; }
```

## `DCC_LISTENER_INTERVAL`

Returns a versioned interval schedule value.

```c
static inline dcc_listener_schedule_target_t DCC_LISTENER_INTERVAL( uint64_t interval_ms ) { dcc_listener_schedule_target_t value = { sizeof(value), DCC_LISTENER_TARGET_VERSION, DCC_LISTENER_SCHEDULE_INTERVAL, interval_ms, 0U, 0U}; return value; }
```

## `DCC_LISTENER_MESSAGE_COMMAND_HANDLER`

Wraps a message-command handler without casting it.

```c
static inline dcc_listener_handler_t DCC_LISTENER_MESSAGE_COMMAND_HANDLER( dcc_app_message_command_fn handler ) { dcc_listener_handler_t value = {0}; value.message_command = handler; return value; }
```

## `DCC_LISTENER_MESSAGE_HANDLER`

Wraps a message event handler without casting it.

```c
static inline dcc_listener_handler_t DCC_LISTENER_MESSAGE_HANDLER( dcc_app_message_fn handler ) { dcc_listener_handler_t value = {0}; value.message = handler; return value; }
```

## `DCC_LISTENER_PLAIN_HANDLER`

Wraps a plain route handler without casting it.

```c
static inline dcc_listener_handler_t DCC_LISTENER_PLAIN_HANDLER( dcc_app_handler_fn handler ) { dcc_listener_handler_t value = {0}; value.plain = handler; return value; }
```

## `DCC_LISTENER_READY_HANDLER`

Wraps a READY handler without casting it.

```c
static inline dcc_listener_handler_t DCC_LISTENER_READY_HANDLER( dcc_app_ready_fn handler ) { dcc_listener_handler_t value = {0}; value.ready = handler; return value; }
```

## `DCC_LISTENER_TASK_HANDLER`

Wraps a scheduled task handler without casting it.

```c
static inline dcc_listener_handler_t DCC_LISTENER_TASK_HANDLER( dcc_app_task_fn handler ) { dcc_listener_handler_t value = {0}; value.task = handler; return value; }
```

## `DCC_LISTENER_TYPED_HANDLER`

Wraps a typed route handler without casting it.

```c
static inline dcc_listener_handler_t DCC_LISTENER_TYPED_HANDLER( dcc_app_typed_handler_fn handler ) { dcc_listener_handler_t value = {0}; value.typed = handler; return value; }
```

## `DCC_LISTEN_AUTOCOMPLETE`

Builds a default-config autocomplete listener.

```c
static inline dcc_listener_t DCC_LISTEN_AUTOCOMPLETE( const char *command_name, const char *description, dcc_app_handler_fn handler ) { return DCC_LISTEN_AUTOCOMPLETE_WITH(command_name, description, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_AUTOCOMPLETE_WITH`

Builds an autocomplete listener with an explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_AUTOCOMPLETE_WITH( const char *command_name, const char *description, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_route_listener(DCC_LISTENER_AUTOCOMPLETE, command_name, description, NULL, NULL, handler, config); }
```

## `DCC_LISTEN_BUTTON`

Builds a default-config exact button listener.

```c
static inline dcc_listener_t DCC_LISTEN_BUTTON( const char *custom_id, dcc_app_handler_fn handler ) { return DCC_LISTEN_BUTTON_WITH(custom_id, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_BUTTON_PREFIX`

Builds a default-config button-prefix listener.

```c
static inline dcc_listener_t DCC_LISTEN_BUTTON_PREFIX( const char *custom_id_prefix, dcc_app_handler_fn handler ) { return DCC_LISTEN_BUTTON_PREFIX_WITH(custom_id_prefix, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_BUTTON_PREFIX_WITH`

Builds a button-prefix listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_BUTTON_PREFIX_WITH( const char *custom_id_prefix, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_component_listener(DCC_LISTENER_BUTTON_PREFIX, custom_id_prefix, handler, config); }
```

## `DCC_LISTEN_BUTTON_WITH`

Builds an exact button listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_BUTTON_WITH( const char *custom_id, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_component_listener(DCC_LISTENER_BUTTON, custom_id, handler, config); }
```

## `DCC_LISTEN_EVENT`

Builds a default-config generic event listener.

```c
static inline dcc_listener_t DCC_LISTEN_EVENT( dcc_event_type_t type, dcc_app_event_fn handler ) { return DCC_LISTEN_EVENT_WITH(type, DCC_LISTENER_EVENT_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_EVENT_WITH`

Builds a generic event listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_EVENT_WITH( dcc_event_type_t type, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_event_listener(DCC_LISTENER_EVENT, type, handler, config); }
```

## `DCC_LISTEN_MESSAGE_COMMAND`

Builds a default-config message-command listener.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_COMMAND( const char *prefix, const char *name, dcc_app_message_command_fn handler ) { return DCC_LISTEN_MESSAGE_COMMAND_WITH(prefix, name, DCC_LISTENER_MESSAGE_COMMAND_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_MESSAGE_COMMAND_WITH`

Builds a message-command listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_COMMAND_WITH( const char *prefix, const char *name, dcc_listener_handler_t handler, dcc_listener_config_t config ) { dcc_listener_t value = dcc_bot_listener_base( DCC_LISTENER_MESSAGE_COMMAND, handler, config); value.target.message_command.prefix = prefix; value.target.message_command.name = name; return value; }
```

## `DCC_LISTEN_MESSAGE_CONTEXT_MENU`

Builds a default-config message context-menu listener.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_CONTEXT_MENU( const char *name, dcc_app_handler_fn handler ) { return DCC_LISTEN_MESSAGE_CONTEXT_MENU_WITH(name, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_MESSAGE_CONTEXT_MENU_WITH`

Builds a message context-menu listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_CONTEXT_MENU_WITH( const char *name, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_route_listener(DCC_LISTENER_MESSAGE_CONTEXT_MENU, name, NULL, NULL, NULL, handler, config); }
```

## `DCC_LISTEN_MESSAGE_CREATE`

Builds a default-config message-create listener.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_CREATE( dcc_app_message_fn handler ) { return DCC_LISTEN_MESSAGE_CREATE_WITH(DCC_LISTENER_MESSAGE_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_MESSAGE_CREATE_WITH`

Builds a message-create listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_CREATE_WITH( dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_event_listener(DCC_LISTENER_MESSAGE_CREATE, DCC_EVENT_MESSAGE_CREATE, handler, config); }
```

## `DCC_LISTEN_MESSAGE_DELETE`

Builds a default-config message-delete listener.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_DELETE( dcc_app_message_fn handler ) { return DCC_LISTEN_MESSAGE_DELETE_WITH(DCC_LISTENER_MESSAGE_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_MESSAGE_DELETE_WITH`

Builds a message-delete listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_DELETE_WITH( dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_event_listener(DCC_LISTENER_MESSAGE_DELETE, DCC_EVENT_MESSAGE_DELETE, handler, config); }
```

## `DCC_LISTEN_MESSAGE_UPDATE`

Builds a default-config message-update listener.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_UPDATE( dcc_app_message_fn handler ) { return DCC_LISTEN_MESSAGE_UPDATE_WITH(DCC_LISTENER_MESSAGE_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_MESSAGE_UPDATE_WITH`

Builds a message-update listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_MESSAGE_UPDATE_WITH( dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_event_listener(DCC_LISTENER_MESSAGE_UPDATE, DCC_EVENT_MESSAGE_UPDATE, handler, config); }
```

## `DCC_LISTEN_MODAL`

Builds a default-config exact modal listener.

```c
static inline dcc_listener_t DCC_LISTEN_MODAL( const char *custom_id, dcc_app_handler_fn handler ) { return DCC_LISTEN_MODAL_WITH(custom_id, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_MODAL_PREFIX`

Builds a default-config modal-prefix listener.

```c
static inline dcc_listener_t DCC_LISTEN_MODAL_PREFIX( const char *custom_id_prefix, dcc_app_handler_fn handler ) { return DCC_LISTEN_MODAL_PREFIX_WITH(custom_id_prefix, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_MODAL_PREFIX_WITH`

Builds a modal-prefix listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_MODAL_PREFIX_WITH( const char *custom_id_prefix, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_component_listener(DCC_LISTENER_MODAL_PREFIX, custom_id_prefix, handler, config); }
```

## `DCC_LISTEN_MODAL_WITH`

Builds an exact modal listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_MODAL_WITH( const char *custom_id, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_component_listener(DCC_LISTENER_MODAL, custom_id, handler, config); }
```

## `DCC_LISTEN_READY`

Builds a default-config READY listener.

```c
static inline dcc_listener_t DCC_LISTEN_READY(dcc_app_ready_fn handler) { return DCC_LISTEN_READY_WITH(DCC_LISTENER_READY_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_READY_WITH`

Builds a READY listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_READY_WITH( dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_event_listener(DCC_LISTENER_READY, DCC_EVENT_READY, handler, config); }
```

## `DCC_LISTEN_SELECT`

Builds a default-config exact select listener.

```c
static inline dcc_listener_t DCC_LISTEN_SELECT( const char *custom_id, dcc_app_handler_fn handler ) { return DCC_LISTEN_SELECT_WITH(custom_id, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_SELECT_PREFIX`

Builds a default-config select-prefix listener.

```c
static inline dcc_listener_t DCC_LISTEN_SELECT_PREFIX( const char *custom_id_prefix, dcc_app_handler_fn handler ) { return DCC_LISTEN_SELECT_PREFIX_WITH(custom_id_prefix, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_SELECT_PREFIX_WITH`

Builds a select-prefix listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_SELECT_PREFIX_WITH( const char *custom_id_prefix, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_component_listener(DCC_LISTENER_SELECT_PREFIX, custom_id_prefix, handler, config); }
```

## `DCC_LISTEN_SELECT_WITH`

Builds an exact select listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_SELECT_WITH( const char *custom_id, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_component_listener(DCC_LISTENER_SELECT, custom_id, handler, config); }
```

## `DCC_LISTEN_SLASH`

Builds a default-config slash listener.

```c
static inline dcc_listener_t DCC_LISTEN_SLASH( const char *name, const char *description, dcc_app_handler_fn handler ) { return DCC_LISTEN_SLASH_WITH(name, description, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_SLASH_WITH`

Builds a slash listener borrowing all metadata until registration returns.

```c
static inline dcc_listener_t DCC_LISTEN_SLASH_WITH( const char *name, const char *description, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_route_listener(DCC_LISTENER_SLASH, name, description, NULL, NULL, handler, config); }
```

## `DCC_LISTEN_SUBCOMMAND`

Builds a default-config subcommand listener.

```c
static inline dcc_listener_t DCC_LISTEN_SUBCOMMAND( const char *command_name, const char *subcommand_path, const char *description, dcc_app_handler_fn handler ) { return DCC_LISTEN_SUBCOMMAND_WITH(command_name, subcommand_path, description, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_SUBCOMMAND_AUTOCOMPLETE`

Builds default-config subcommand autocomplete.

```c
static inline dcc_listener_t DCC_LISTEN_SUBCOMMAND_AUTOCOMPLETE( const char *command_name, const char *subcommand_path, const char *description, dcc_app_handler_fn handler ) { return DCC_LISTEN_SUBCOMMAND_AUTOCOMPLETE_WITH(command_name, subcommand_path, description, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_SUBCOMMAND_AUTOCOMPLETE_WITH`

Builds subcommand autocomplete with an explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_SUBCOMMAND_AUTOCOMPLETE_WITH( const char *command_name, const char *subcommand_path, const char *description, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_route_listener(DCC_LISTENER_AUTOCOMPLETE, NULL, description, command_name, subcommand_path, handler, config); }
```

## `DCC_LISTEN_SUBCOMMAND_WITH`

Builds a subcommand listener with an explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_SUBCOMMAND_WITH( const char *command_name, const char *subcommand_path, const char *description, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_route_listener(DCC_LISTENER_SUBCOMMAND, NULL, description, command_name, subcommand_path, handler, config); }
```

## `DCC_LISTEN_TASK`

Builds a default-config task listener.

```c
static inline dcc_listener_t DCC_LISTEN_TASK( dcc_listener_schedule_target_t schedule, dcc_app_task_fn handler ) { return DCC_LISTEN_TASK_WITH(schedule, DCC_LISTENER_TASK_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_TASK_WITH`

Builds a task listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_TASK_WITH( dcc_listener_schedule_target_t schedule, dcc_listener_handler_t handler, dcc_listener_config_t config ) { dcc_listener_t value = dcc_bot_listener_base(DCC_LISTENER_TASK, handler, config); value.target.schedule = schedule; return value; }
```

## `DCC_LISTEN_USER_CONTEXT_MENU`

Builds a default-config user context-menu listener.

```c
static inline dcc_listener_t DCC_LISTEN_USER_CONTEXT_MENU( const char *name, dcc_app_handler_fn handler ) { return DCC_LISTEN_USER_CONTEXT_MENU_WITH(name, DCC_LISTENER_PLAIN_HANDLER(handler), DCC_LISTENER_CONFIG()); }
```

## `DCC_LISTEN_USER_CONTEXT_MENU_WITH`

Builds a user context-menu listener with explicit configuration.

```c
static inline dcc_listener_t DCC_LISTEN_USER_CONTEXT_MENU_WITH( const char *name, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_route_listener(DCC_LISTENER_USER_CONTEXT_MENU, name, NULL, NULL, NULL, handler, config); }
```

## `dcc_bot_component_listener`

Public DCC API declaration for `dcc_bot_component_listener`.

```c
static inline dcc_listener_t dcc_bot_component_listener( dcc_listener_kind_t kind, const char *custom_id, dcc_listener_handler_t handler, dcc_listener_config_t config ) { return dcc_bot_route_listener(kind, custom_id, NULL, NULL, NULL, handler, config); }
```

## `dcc_bot_event_listener`

Public DCC API declaration for `dcc_bot_event_listener`.

```c
static inline dcc_listener_t dcc_bot_event_listener( dcc_listener_kind_t kind, dcc_event_type_t type, dcc_listener_handler_t handler, dcc_listener_config_t config ) { dcc_listener_t value = dcc_bot_listener_base(kind, handler, config); value.target.event.type = type; return value; }
```

## `dcc_bot_listener_base`

Public DCC API declaration for `dcc_bot_listener_base`.

```c
static inline dcc_listener_t dcc_bot_listener_base( dcc_listener_kind_t kind, dcc_listener_handler_t handler, dcc_listener_config_t config ) { dcc_listener_t value; dcc_listener_init(&value, kind); if (config.size < sizeof(config) || config.version != DCC_LISTENER_CONFIG_VERSION || (config.once != 0U && !(kind == DCC_LISTENER_EVENT || kind == DCC_LISTENER_READY || (kind >= DCC_LISTENER_MESSAGE_CREATE && kind <= DCC_LISTENER_MESSAGE_DELETE)))) { value.size = 0U; return value; } value.handler = handler; value.user_data = config.user_data; value.cleanup = config.cleanup; value.policy = config.policy; value.args_size = config.args_size; value.bindings = config.bindings; value.validators = config.validators; value.validation = config.validation; if (kind == DCC_LISTENER_EVENT || kind == DCC_LISTENER_READY || (kind >= DCC_LISTENER_MESSAGE_CREATE && kind <= DCC_LISTENER_MESSAGE_DELETE)) { value.target.event.once = config.once; } return value; }
```

## `dcc_bot_route_listener`

Public DCC API declaration for `dcc_bot_route_listener`.

```c
static inline dcc_listener_t dcc_bot_route_listener( dcc_listener_kind_t kind, const char *name, const char *description, const char *command_name, const char *subcommand_path, dcc_listener_handler_t handler, dcc_listener_config_t config ) { dcc_listener_t value = dcc_bot_listener_base(kind, handler, config); if (config.command != NULL) { value.target.route.command = config.command; value.target.route.command_name = command_name; value.target.route.subcommand_path = subcommand_path; } else { value.target.route.name = name; value.target.route.description = description; value.target.route.command_name = command_name; value.target.route.subcommand_path = subcommand_path; } return value; }
```
