<!-- Generated by tools/generate_api_reference.py; do not edit. -->

# `<dcc/client.h>`

Source: [`include/dcc/client.h`](https://github.com/Feralthedogg/DCC/blob/v2.0.0/include/dcc/client.h)

## `DCC_GATEWAY_INFO_HAS_SESSION_STATE`

Public macro `DCC_GATEWAY_INFO_HAS_SESSION_STATE`; arguments follow the owner header contract.

```c
#define DCC_GATEWAY_INFO_HAS_SESSION_STATE ...
```

## `dcc_client_destroy`

@file include/dcc/client.h @brief Public DCC client declarations.

```c
#ifndef DCC_CLIENT_H #define DCC_CLIENT_H #include <dcc/error.h> #include <dcc/error_details.h> #include <dcc/events.h> #include <dcc/export.h> #include <dcc/intents.h> #include <dcc/snowflake.h> #include <stddef.h> #include <stdint.h> #define DCC_GATEWAY_INFO_HAS_SESSION_STATE 1 #ifdef __cplusplus extern "C" { #endif typedef void (*dcc_log_fn)(dcc_log_level_t level, const char *message, void *user_data); typedef void (*dcc_client_error_fn)(dcc_client_t *client, const dcc_error_t *error, void *user_data); typedef struct dcc_client_options { size_t size; const char *token; dcc_intents_t intents; uint32_t shard_id; uint32_t shard_count; uint32_t rest_concurrency; uint32_t enable_cache; dcc_log_fn log_fn; void *log_user_data; const char *gateway_url; size_t cache_max_guilds; size_t cache_max_channels; size_t cache_max_roles; size_t cache_max_members; size_t cache_max_users; size_t cache_max_messages; size_t cache_max_voice_states; uint32_t gateway_max_concurrency; uint32_t infer_guild_id_from_channel; size_t rest_max_queued_requests; size_t rest_max_request_bytes; size_t rest_max_queued_bytes; size_t rest_max_active_bytes; size_t rest_max_response_bytes; size_t interaction_max_live_queues; size_t interaction_max_tombstones; uint64_t interaction_tombstone_ttl_ms; size_t interaction_max_actions_per_queue; size_t interaction_max_reserved_bytes_per_queue; size_t interaction_max_reserved_bytes_total; } dcc_client_options_t; typedef struct dcc_runtime_stats { size_t size; uint64_t tasks_spawned; uint64_t events_dispatched; } dcc_runtime_stats_t; typedef struct dcc_gateway_info { size_t size; const char *url; const char *resume_url; uint32_t recommended_shards; uint32_t max_concurrency; uint32_t remaining_identifies; uint32_t total_identifies; uint64_t reset_after_ms; uint64_t reset_at_ms; const char *session_id; uint64_t seq; uint32_t identify_delay_ms; dcc_status_t last_status; uint8_t has_seq; uint8_t can_resume; uint8_t task_running; uint8_t reconnect_requested; uint8_t reconnect_resume; } dcc_gateway_info_t; typedef struct dcc_gateway_request_guild_members_options { size_t size; dcc_snowflake_t guild_id; const char *query; uint32_t limit; uint8_t presences; const dcc_snowflake_t *user_ids; size_t user_id_count; const char *nonce; } dcc_gateway_request_guild_members_options_t; typedef struct dcc_gateway_presence_update { size_t size; const char * status; int64_t since_ms; uint8_t has_since; uint8_t afk; const char *activities_json; } dcc_gateway_presence_update_t; DCC_API dcc_status_t dcc_client_create(const dcc_client_options_t *options, dcc_client_t **out);
```

## `dcc_client_destroy`

@brief Stops and releases a client. Passing NULL is allowed and has no effect. When called from a managed LLAM task, including a REST terminal callback, terminal error log callback, or structured REST error observer for this client, this function requests stop and returns without releasing memory. The owning unmanaged thread must call it again after that task or callback returns. If the configured log callback reports that destruction was deferred because runtime quiescence failed, the allocation is likewise preserved with new start, wait, and REST admission closed. Let any existing runtime driver and client wait call return, then retry destruction from the owning unmanaged thread. In all other cases, treat the pointer as invalid after this function returns. Callers must synchronize destruction with any concurrent client API calls they own.

```c
DCC_API void dcc_client_destroy(dcc_client_t *client);
```

## `dcc_client_gateway_info`

@brief Copies gateway state into an output snapshot. The caller must initialize out->size before calling.

```c
DCC_API dcc_status_t dcc_client_gateway_info(const dcc_client_t *client, dcc_gateway_info_t *out);
```

## `dcc_client_gateway_reconnect`

@brief Requests a gateway reconnect. @param resume Non-zero to attempt gateway session resume when possible.

```c
DCC_API dcc_status_t dcc_client_gateway_reconnect(dcc_client_t *client, uint8_t resume);
```

## `dcc_client_gateway_start`

@brief Starts the gateway loop for an already-created client.

```c
DCC_API dcc_status_t dcc_client_gateway_start(dcc_client_t *client);
```

## `dcc_client_gateway_stop`

@brief Stops the gateway loop without destroying the client.

```c
DCC_API dcc_status_t dcc_client_gateway_stop(dcc_client_t *client);
```

## `dcc_client_get_dm_channel`

@brief Returns the cached DM channel ID for a user, or zero when unknown.

```c
DCC_API dcc_snowflake_t dcc_client_get_dm_channel(const dcc_client_t *client, dcc_snowflake_t user_id);
```

## `dcc_client_infer_guild_id_from_channel`

@brief Looks up the guild that owns a cached channel.

```c
DCC_API dcc_status_t dcc_client_infer_guild_id_from_channel( const dcc_client_t *client, dcc_snowflake_t channel_id, dcc_snowflake_t *out_guild_id);
```

## `dcc_client_last_error`

@brief Returns the client's last diagnostic error message. The returned pointer is owned by the client. A null client returns an empty string.

```c
DCC_API const char *dcc_client_last_error(const dcc_client_t *client);
```

## `dcc_client_on_error`

Installs the public structured error observer for a client. (NULL, NULL) clears the observer. A NULL callback with non-NULL user data is invalid. This public slot is independent from an App-owned internal sink.

```c
DCC_API dcc_status_t dcc_client_on_error(dcc_client_t *client, dcc_client_error_fn handler, void *user_data);
```

## `dcc_client_request_channel_info`

@brief Sends Gateway Request Channel Info (opcode 43).

```c
DCC_API dcc_status_t dcc_client_request_channel_info(dcc_client_t *client, dcc_snowflake_t guild_id, const char *const *fields, size_t field_count);
```

## `dcc_client_request_guild_members`

@brief Sends Gateway Request Guild Members (opcode 8).

```c
DCC_API dcc_status_t dcc_client_request_guild_members( dcc_client_t *client, const dcc_gateway_request_guild_members_options_t *options);
```

## `dcc_client_request_soundboard_sounds`

@brief Sends Gateway Request Soundboard Sounds (opcode 31).

```c
DCC_API dcc_status_t dcc_client_request_soundboard_sounds( dcc_client_t *client, const dcc_snowflake_t *guild_ids, size_t guild_id_count);
```

## `dcc_client_runtime_stats`

@brief Copies runtime counters into an output snapshot. The caller must initialize out->size before calling.

```c
DCC_API dcc_status_t dcc_client_runtime_stats(dcc_client_t *client, dcc_runtime_stats_t *out);
```

## `dcc_client_set_dm_channel`

@brief Stores or updates a user-to-DM-channel mapping in the client cache.

```c
DCC_API dcc_status_t dcc_client_set_dm_channel(dcc_client_t *client, dcc_snowflake_t user_id, dcc_snowflake_t channel_id);
```

## `dcc_client_start`

@brief Starts the client's runtime services.

```c
DCC_API dcc_status_t dcc_client_start(dcc_client_t *client);
```

## `dcc_client_stop`

@brief Requests client shutdown.

```c
DCC_API dcc_status_t dcc_client_stop(dcc_client_t *client);
```

## `dcc_client_update_presence`

@brief Sends Gateway Update Presence (opcode 3).

```c
DCC_API dcc_status_t dcc_client_update_presence( dcc_client_t *client, const dcc_gateway_presence_update_t *presence);
```

## `dcc_client_update_presence_raw`

@brief Sends Gateway Update Presence (opcode 3) with a raw presence object. presence_json must be the inner d object, not the full Gateway payload.

```c
DCC_API dcc_status_t dcc_client_update_presence_raw(dcc_client_t *client, const char *presence_json);
```

## `dcc_client_update_voice_state`

@brief Sends a gateway voice-state update for the current user.

```c
DCC_API dcc_status_t dcc_client_update_voice_state(dcc_client_t *client, dcc_snowflake_t guild_id, dcc_snowflake_t channel_id, uint8_t self_mute, uint8_t self_deaf);
```

## `dcc_client_wait`

@brief Blocks until the client runtime finishes.

```c
DCC_API dcc_status_t dcc_client_wait(dcc_client_t *client);
```
