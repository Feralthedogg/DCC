<!-- Generated by tools/generate_api_reference.py; do not edit. -->

# `<dcc/objects/clone/resources.h>`

Source: [`include/dcc/objects/clone/resources.h`](https://github.com/Feralthedogg/DCC/blob/v2.0.1/include/dcc/objects/clone/resources.h)

## `dcc_channel_clone`

Public DCC API declaration for `dcc_channel_clone`.

```c
DCC_API dcc_channel_t *dcc_channel_clone(const dcc_channel_t *channel);
```

## `dcc_channel_free`

Public DCC API declaration for `dcc_channel_free`.

```c
DCC_API void dcc_channel_free(dcc_channel_t *channel);
```

## `dcc_emoji_clone`

Public DCC API declaration for `dcc_emoji_clone`.

```c
DCC_API dcc_emoji_t *dcc_emoji_clone(const dcc_emoji_t *emoji);
```

## `dcc_emoji_free`

Public DCC API declaration for `dcc_emoji_free`.

```c
DCC_API void dcc_emoji_free(dcc_emoji_t *emoji);
```

## `dcc_entitlement_clone`

Public DCC API declaration for `dcc_entitlement_clone`.

```c
DCC_API dcc_entitlement_t *dcc_entitlement_clone(const dcc_entitlement_t *entitlement);
```

## `dcc_entitlement_free`

Public DCC API declaration for `dcc_entitlement_free`.

```c
DCC_API void dcc_entitlement_free(dcc_entitlement_t *entitlement);
```

## `dcc_guild_clone`

Public DCC API declaration for `dcc_guild_clone`.

```c
DCC_API dcc_guild_t *dcc_guild_clone(const dcc_guild_t *guild);
```

## `dcc_guild_free`

Public DCC API declaration for `dcc_guild_free`.

```c
DCC_API void dcc_guild_free(dcc_guild_t *guild);
```

## `dcc_invite_clone`

Public DCC API declaration for `dcc_invite_clone`.

```c
DCC_API dcc_invite_t *dcc_invite_clone(const dcc_invite_t *invite);
```

## `dcc_invite_free`

Public DCC API declaration for `dcc_invite_free`.

```c
DCC_API void dcc_invite_free(dcc_invite_t *invite);
```

## `dcc_member_clone`

Public DCC API declaration for `dcc_member_clone`.

```c
DCC_API dcc_member_t *dcc_member_clone(const dcc_member_t *member);
```

## `dcc_member_free`

Public DCC API declaration for `dcc_member_free`.

```c
DCC_API void dcc_member_free(dcc_member_t *member);
```

## `dcc_role_clone`

Public DCC API declaration for `dcc_role_clone`.

```c
DCC_API dcc_role_t *dcc_role_clone(const dcc_role_t *role);
```

## `dcc_role_free`

Public DCC API declaration for `dcc_role_free`.

```c
DCC_API void dcc_role_free(dcc_role_t *role);
```

## `dcc_scheduled_event_clone`

Public DCC API declaration for `dcc_scheduled_event_clone`.

```c
DCC_API dcc_scheduled_event_t *dcc_scheduled_event_clone(const dcc_scheduled_event_t *scheduled_event);
```

## `dcc_scheduled_event_free`

Public DCC API declaration for `dcc_scheduled_event_free`.

```c
DCC_API void dcc_scheduled_event_free(dcc_scheduled_event_t *scheduled_event);
```

## `dcc_soundboard_sound_clone`

Public DCC API declaration for `dcc_soundboard_sound_clone`.

```c
DCC_API dcc_soundboard_sound_t *dcc_soundboard_sound_clone(const dcc_soundboard_sound_t *soundboard_sound);
```

## `dcc_soundboard_sound_free`

Public DCC API declaration for `dcc_soundboard_sound_free`.

```c
DCC_API void dcc_soundboard_sound_free(dcc_soundboard_sound_t *soundboard_sound);
```

## `dcc_stage_instance_clone`

Public DCC API declaration for `dcc_stage_instance_clone`.

```c
DCC_API dcc_stage_instance_t *dcc_stage_instance_clone(const dcc_stage_instance_t *stage_instance);
```

## `dcc_stage_instance_free`

Public DCC API declaration for `dcc_stage_instance_free`.

```c
DCC_API void dcc_stage_instance_free(dcc_stage_instance_t *stage_instance);
```

## `dcc_sticker_clone`

Public DCC API declaration for `dcc_sticker_clone`.

```c
DCC_API dcc_sticker_t *dcc_sticker_clone(const dcc_sticker_t *sticker);
```

## `dcc_sticker_free`

Public DCC API declaration for `dcc_sticker_free`.

```c
DCC_API void dcc_sticker_free(dcc_sticker_t *sticker);
```

## `dcc_subscription_clone`

Public DCC API declaration for `dcc_subscription_clone`.

```c
DCC_API dcc_subscription_t *dcc_subscription_clone(const dcc_subscription_t *subscription);
```

## `dcc_subscription_free`

Public DCC API declaration for `dcc_subscription_free`.

```c
DCC_API void dcc_subscription_free(dcc_subscription_t *subscription);
```
