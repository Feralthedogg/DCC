<!-- Generated by tools/generate_api_reference.py; do not edit. -->

# `<dcc/embed.h>`

Source: [`include/dcc/embed.h`](https://github.com/Feralthedogg/DCC/blob/v2.0.1/include/dcc/embed.h)

## `DCC_EMBED_BUILDER_INIT`

Public macro `DCC_EMBED_BUILDER_INIT`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_INIT ...
```

## `DCC_EMBED_BUILDER_PRESENT_AUTHOR`

Public macro `DCC_EMBED_BUILDER_PRESENT_AUTHOR`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_AUTHOR ...
```

## `DCC_EMBED_BUILDER_PRESENT_COLOR`

Public macro `DCC_EMBED_BUILDER_PRESENT_COLOR`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_COLOR ...
```

## `DCC_EMBED_BUILDER_PRESENT_DESCRIPTION`

Public macro `DCC_EMBED_BUILDER_PRESENT_DESCRIPTION`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_DESCRIPTION ...
```

## `DCC_EMBED_BUILDER_PRESENT_FIELDS`

Public macro `DCC_EMBED_BUILDER_PRESENT_FIELDS`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_FIELDS ...
```

## `DCC_EMBED_BUILDER_PRESENT_FOOTER`

Public macro `DCC_EMBED_BUILDER_PRESENT_FOOTER`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_FOOTER ...
```

## `DCC_EMBED_BUILDER_PRESENT_IMAGE`

Public macro `DCC_EMBED_BUILDER_PRESENT_IMAGE`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_IMAGE ...
```

## `DCC_EMBED_BUILDER_PRESENT_THUMBNAIL`

Public macro `DCC_EMBED_BUILDER_PRESENT_THUMBNAIL`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_THUMBNAIL ...
```

## `DCC_EMBED_BUILDER_PRESENT_TIMESTAMP`

Public macro `DCC_EMBED_BUILDER_PRESENT_TIMESTAMP`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_TIMESTAMP ...
```

## `DCC_EMBED_BUILDER_PRESENT_TITLE`

Public macro `DCC_EMBED_BUILDER_PRESENT_TITLE`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_TITLE ...
```

## `DCC_EMBED_BUILDER_PRESENT_TYPE`

Public macro `DCC_EMBED_BUILDER_PRESENT_TYPE`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_TYPE ...
```

## `DCC_EMBED_BUILDER_PRESENT_URL`

Public macro `DCC_EMBED_BUILDER_PRESENT_URL`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_PRESENT_URL ...
```

## `DCC_EMBED_BUILDER_VERSION`

Public macro `DCC_EMBED_BUILDER_VERSION`; arguments follow the owner header contract.

```c
#define DCC_EMBED_BUILDER_VERSION ...
```

## `dcc_embed_builder_build_array_json`

Public DCC API declaration for `dcc_embed_builder_build_array_json`.

```c
DCC_API dcc_status_t dcc_embed_builder_build_array_json( const dcc_embed_builder_t *builders, size_t builder_count, char **out_json );
```

## `dcc_embed_builder_build_json`

Public DCC API declaration for `dcc_embed_builder_build_json`.

```c
DCC_API dcc_status_t dcc_embed_builder_build_json(const dcc_embed_builder_t *builder, char **out_json);
```

## `dcc_embed_builder_init`

Public DCC API declaration for `dcc_embed_builder_init`.

```c
DCC_API void dcc_embed_builder_init(dcc_embed_builder_t *builder);
```

## `dcc_embed_builder_json_free`

Public DCC API declaration for `dcc_embed_builder_json_free`.

```c
DCC_API void dcc_embed_builder_json_free(char *json);
```

## `dcc_embed_builder_set_author`

Public DCC API declaration for `dcc_embed_builder_set_author`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_author( dcc_embed_builder_t *builder, const char *name, const char *url, const char *icon_url );
```

## `dcc_embed_builder_set_color`

Public DCC API declaration for `dcc_embed_builder_set_color`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_color(dcc_embed_builder_t *builder, uint32_t color);
```

## `dcc_embed_builder_set_description`

Public DCC API declaration for `dcc_embed_builder_set_description`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_description(dcc_embed_builder_t *builder, const char *description);
```

## `dcc_embed_builder_set_fields`

Public DCC API declaration for `dcc_embed_builder_set_fields`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_fields( dcc_embed_builder_t *builder, const dcc_embed_field_t *fields, size_t field_count );
```

## `dcc_embed_builder_set_footer`

Public DCC API declaration for `dcc_embed_builder_set_footer`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_footer( dcc_embed_builder_t *builder, const char *text, const char *icon_url );
```

## `dcc_embed_builder_set_image`

Public DCC API declaration for `dcc_embed_builder_set_image`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_image(dcc_embed_builder_t *builder, const char *url);
```

## `dcc_embed_builder_set_thumbnail`

Public DCC API declaration for `dcc_embed_builder_set_thumbnail`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_thumbnail(dcc_embed_builder_t *builder, const char *url);
```

## `dcc_embed_builder_set_timestamp`

Public DCC API declaration for `dcc_embed_builder_set_timestamp`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_timestamp(dcc_embed_builder_t *builder, const char *timestamp);
```

## `dcc_embed_builder_set_title`

Public DCC API declaration for `dcc_embed_builder_set_title`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_title(dcc_embed_builder_t *builder, const char *title);
```

## `dcc_embed_builder_set_type`

Public DCC API declaration for `dcc_embed_builder_set_type`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_type(dcc_embed_builder_t *builder, const char *type);
```

## `dcc_embed_builder_set_url`

Public DCC API declaration for `dcc_embed_builder_set_url`.

```c
DCC_API dcc_status_t dcc_embed_builder_set_url(dcc_embed_builder_t *builder, const char *url);
```
