<!-- Generated by tools/generate_api_reference.py; do not edit. -->

# `<dcc/modal.h>`

Source: [`include/dcc/modal.h`](https://github.com/Feralthedogg/DCC/blob/v2.0.0/include/dcc/modal.h)

## `DCC_MODAL_BUILDER_INIT`

Public macro `DCC_MODAL_BUILDER_INIT`; arguments follow the owner header contract.

```c
#define DCC_MODAL_BUILDER_INIT ...
```

## `DCC_MODAL_BUILDER_PRESENT_COMPONENTS`

Public macro `DCC_MODAL_BUILDER_PRESENT_COMPONENTS`; arguments follow the owner header contract.

```c
#define DCC_MODAL_BUILDER_PRESENT_COMPONENTS ...
```

## `DCC_MODAL_BUILDER_PRESENT_COMPONENTS_JSON`

Public macro `DCC_MODAL_BUILDER_PRESENT_COMPONENTS_JSON`; arguments follow the owner header contract.

```c
#define DCC_MODAL_BUILDER_PRESENT_COMPONENTS_JSON ...
```

## `DCC_MODAL_BUILDER_PRESENT_COMPONENTS_V2`

Public macro `DCC_MODAL_BUILDER_PRESENT_COMPONENTS_V2`; arguments follow the owner header contract.

```c
#define DCC_MODAL_BUILDER_PRESENT_COMPONENTS_V2 ...
```

## `DCC_MODAL_BUILDER_PRESENT_COMPONENTS_V2_JSON`

Public macro `DCC_MODAL_BUILDER_PRESENT_COMPONENTS_V2_JSON`; arguments follow the owner header contract.

```c
#define DCC_MODAL_BUILDER_PRESENT_COMPONENTS_V2_JSON ...
```

## `DCC_MODAL_BUILDER_PRESENT_CUSTOM_ID`

Public macro `DCC_MODAL_BUILDER_PRESENT_CUSTOM_ID`; arguments follow the owner header contract.

```c
#define DCC_MODAL_BUILDER_PRESENT_CUSTOM_ID ...
```

## `DCC_MODAL_BUILDER_PRESENT_TITLE`

Public macro `DCC_MODAL_BUILDER_PRESENT_TITLE`; arguments follow the owner header contract.

```c
#define DCC_MODAL_BUILDER_PRESENT_TITLE ...
```

## `DCC_MODAL_BUILDER_VERSION`

Public macro `DCC_MODAL_BUILDER_VERSION`; arguments follow the owner header contract.

```c
#define DCC_MODAL_BUILDER_VERSION ...
```

## `dcc_modal_builder_build_json`

Public DCC API declaration for `dcc_modal_builder_build_json`.

```c
DCC_API dcc_status_t dcc_modal_builder_build_json(const dcc_modal_builder_t *builder, char **out_json);
```

## `dcc_modal_builder_init`

Public DCC API declaration for `dcc_modal_builder_init`.

```c
DCC_API void dcc_modal_builder_init(dcc_modal_builder_t *builder);
```

## `dcc_modal_builder_json_free`

Public DCC API declaration for `dcc_modal_builder_json_free`.

```c
DCC_API void dcc_modal_builder_json_free(char *json);
```

## `dcc_modal_builder_set_components`

Public DCC API declaration for `dcc_modal_builder_set_components`.

```c
DCC_API dcc_status_t dcc_modal_builder_set_components( dcc_modal_builder_t *builder, const dcc_component_builder_t *components, size_t components_count );
```

## `dcc_modal_builder_set_components_json`

Public DCC API declaration for `dcc_modal_builder_set_components_json`.

```c
DCC_API dcc_status_t dcc_modal_builder_set_components_json( dcc_modal_builder_t *builder, const char *components_json );
```

## `dcc_modal_builder_set_components_v2`

Public DCC API declaration for `dcc_modal_builder_set_components_v2`.

```c
DCC_API dcc_status_t dcc_modal_builder_set_components_v2( dcc_modal_builder_t *builder, const dcc_component_v2_builder_t *components, size_t components_count );
```

## `dcc_modal_builder_set_components_v2_json`

Public DCC API declaration for `dcc_modal_builder_set_components_v2_json`.

```c
DCC_API dcc_status_t dcc_modal_builder_set_components_v2_json( dcc_modal_builder_t *builder, const char *components_json );
```

## `dcc_modal_builder_set_custom_id`

Public DCC API declaration for `dcc_modal_builder_set_custom_id`.

```c
DCC_API dcc_status_t dcc_modal_builder_set_custom_id(dcc_modal_builder_t *builder, const char *custom_id);
```

## `dcc_modal_builder_set_title`

Public DCC API declaration for `dcc_modal_builder_set_title`.

```c
DCC_API dcc_status_t dcc_modal_builder_set_title(dcc_modal_builder_t *builder, const char *title);
```
