<!-- Generated by tools/generate_api_reference.py; do not edit. -->

# `<dcc/rest/types/interactions.h>`

Source: [`include/dcc/rest/types/interactions.h`](https://github.com/Feralthedogg/DCC/blob/v2.0.1/include/dcc/rest/types/interactions.h)

## `DCC_REST_INTERACTION_RESPONSE_INIT`

Public macro `DCC_REST_INTERACTION_RESPONSE_INIT`; arguments follow the owner header contract.

```c
#define DCC_REST_INTERACTION_RESPONSE_INIT ...
```

## `DCC_REST_INTERACTION_RESPONSE_PRESENT_AUTOCOMPLETE`

Public macro `DCC_REST_INTERACTION_RESPONSE_PRESENT_AUTOCOMPLETE`; arguments follow the owner header contract.

```c
#define DCC_REST_INTERACTION_RESPONSE_PRESENT_AUTOCOMPLETE ...
```

## `DCC_REST_INTERACTION_RESPONSE_PRESENT_MESSAGE`

Public macro `DCC_REST_INTERACTION_RESPONSE_PRESENT_MESSAGE`; arguments follow the owner header contract.

```c
#define DCC_REST_INTERACTION_RESPONSE_PRESENT_MESSAGE ...
```

## `DCC_REST_INTERACTION_RESPONSE_PRESENT_MODAL`

Public macro `DCC_REST_INTERACTION_RESPONSE_PRESENT_MODAL`; arguments follow the owner header contract.

```c
#define DCC_REST_INTERACTION_RESPONSE_PRESENT_MODAL ...
```

## `DCC_REST_INTERACTION_RESPONSE_PRESENT_WITH_RESPONSE`

Public macro `DCC_REST_INTERACTION_RESPONSE_PRESENT_WITH_RESPONSE`; arguments follow the owner header contract.

```c
#define DCC_REST_INTERACTION_RESPONSE_PRESENT_WITH_RESPONSE ...
```

## `DCC_REST_INTERACTION_RESPONSE_VERSION`

Public macro `DCC_REST_INTERACTION_RESPONSE_VERSION`; arguments follow the owner header contract.

```c
#define DCC_REST_INTERACTION_RESPONSE_VERSION ...
```

## `dcc_rest_interaction_response_init`

Public DCC API declaration for `dcc_rest_interaction_response_init`.

```c
static inline void dcc_rest_interaction_response_init( dcc_rest_interaction_response_t *response ) { if (response != NULL) { dcc_rest_interaction_response_t value = DCC_REST_INTERACTION_RESPONSE_INIT; *response = value; } }
```

## `dcc_rest_interaction_response_set_autocomplete`

Public DCC API declaration for `dcc_rest_interaction_response_set_autocomplete`.

```c
static inline dcc_status_t dcc_rest_interaction_response_set_autocomplete( dcc_rest_interaction_response_t *response, const dcc_autocomplete_builder_t *autocomplete ) { if (response == NULL || autocomplete == NULL) return DCC_ERR_INVALID_ARG; dcc_rest_interaction_response_init(response); response->type = DCC_INTERACTION_RESPONSE_AUTOCOMPLETE; response->present = DCC_REST_INTERACTION_RESPONSE_PRESENT_AUTOCOMPLETE; response->data.autocomplete = autocomplete; return DCC_OK; }
```

## `dcc_rest_interaction_response_set_deferred_message`

Public DCC API declaration for `dcc_rest_interaction_response_set_deferred_message`.

```c
static inline dcc_status_t dcc_rest_interaction_response_set_deferred_message( dcc_rest_interaction_response_t *response, const dcc_message_builder_t *message ) { if (response == NULL) return DCC_ERR_INVALID_ARG; dcc_rest_interaction_response_init(response); response->type = DCC_INTERACTION_RESPONSE_DEFERRED_CHANNEL_MESSAGE_WITH_SOURCE; if (message != NULL) { response->present = DCC_REST_INTERACTION_RESPONSE_PRESENT_MESSAGE; response->data.message = message; } return DCC_OK; }
```

## `dcc_rest_interaction_response_set_deferred_update`

Public DCC API declaration for `dcc_rest_interaction_response_set_deferred_update`.

```c
static inline dcc_status_t dcc_rest_interaction_response_set_deferred_update( dcc_rest_interaction_response_t *response ) { if (response == NULL) return DCC_ERR_INVALID_ARG; dcc_rest_interaction_response_init(response); response->type = DCC_INTERACTION_RESPONSE_DEFERRED_UPDATE_MESSAGE; return DCC_OK; }
```

## `dcc_rest_interaction_response_set_launch_activity`

Public DCC API declaration for `dcc_rest_interaction_response_set_launch_activity`.

```c
static inline dcc_status_t dcc_rest_interaction_response_set_launch_activity( dcc_rest_interaction_response_t *response ) { if (response == NULL) return DCC_ERR_INVALID_ARG; dcc_rest_interaction_response_init(response); response->type = DCC_INTERACTION_RESPONSE_LAUNCH_ACTIVITY; return DCC_OK; }
```

## `dcc_rest_interaction_response_set_message`

Public DCC API declaration for `dcc_rest_interaction_response_set_message`.

```c
static inline dcc_status_t dcc_rest_interaction_response_set_message( dcc_rest_interaction_response_t *response, const dcc_message_builder_t *message ) { if (response == NULL || message == NULL) return DCC_ERR_INVALID_ARG; dcc_rest_interaction_response_init(response); response->type = DCC_INTERACTION_RESPONSE_CHANNEL_MESSAGE_WITH_SOURCE; response->present = DCC_REST_INTERACTION_RESPONSE_PRESENT_MESSAGE; response->data.message = message; return DCC_OK; }
```

## `dcc_rest_interaction_response_set_modal`

Public DCC API declaration for `dcc_rest_interaction_response_set_modal`.

```c
static inline dcc_status_t dcc_rest_interaction_response_set_modal( dcc_rest_interaction_response_t *response, const dcc_modal_builder_t *modal ) { if (response == NULL || modal == NULL) return DCC_ERR_INVALID_ARG; dcc_rest_interaction_response_init(response); response->type = DCC_INTERACTION_RESPONSE_MODAL; response->present = DCC_REST_INTERACTION_RESPONSE_PRESENT_MODAL; response->data.modal = modal; return DCC_OK; }
```

## `dcc_rest_interaction_response_set_pong`

Public DCC API declaration for `dcc_rest_interaction_response_set_pong`.

```c
static inline dcc_status_t dcc_rest_interaction_response_set_pong( dcc_rest_interaction_response_t *response ) { if (response == NULL) return DCC_ERR_INVALID_ARG; dcc_rest_interaction_response_init(response); response->type = DCC_INTERACTION_RESPONSE_PONG; return DCC_OK; }
```

## `dcc_rest_interaction_response_set_premium_required`

Public DCC API declaration for `dcc_rest_interaction_response_set_premium_required`.

```c
static inline dcc_status_t dcc_rest_interaction_response_set_premium_required( dcc_rest_interaction_response_t *response ) { if (response == NULL) return DCC_ERR_INVALID_ARG; dcc_rest_interaction_response_init(response); response->type = DCC_INTERACTION_RESPONSE_PREMIUM_REQUIRED; return DCC_OK; }
```

## `dcc_rest_interaction_response_set_update_message`

Public DCC API declaration for `dcc_rest_interaction_response_set_update_message`.

```c
static inline dcc_status_t dcc_rest_interaction_response_set_update_message( dcc_rest_interaction_response_t *response, const dcc_message_builder_t *message ) { if (response == NULL || message == NULL) return DCC_ERR_INVALID_ARG; dcc_rest_interaction_response_init(response); response->type = DCC_INTERACTION_RESPONSE_UPDATE_MESSAGE; response->present = DCC_REST_INTERACTION_RESPONSE_PRESENT_MESSAGE; response->data.message = message; return DCC_OK; }
```
