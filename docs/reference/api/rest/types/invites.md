<!-- Generated by tools/generate_api_reference.py; do not edit. -->

# `<dcc/rest/types/invites.h>`

Source: [`include/dcc/rest/types/invites.h`](https://github.com/Feralthedogg/DCC/blob/v2.0.1/include/dcc/rest/types/invites.h)

## `DCC_INVITE_PARAMS_INIT`

Public macro `DCC_INVITE_PARAMS_INIT`; arguments follow the owner header contract.

```c
#define DCC_INVITE_PARAMS_INIT ...
```

## `DCC_INVITE_PARAMS_VERSION`

Public macro `DCC_INVITE_PARAMS_VERSION`; arguments follow the owner header contract.

```c
#define DCC_INVITE_PARAMS_VERSION ...
```

## `DCC_INVITE_RECORD_INIT_VALUE`

Public macro `DCC_INVITE_RECORD_INIT_VALUE`; arguments follow the owner header contract.

```c
#define DCC_INVITE_RECORD_INIT_VALUE ...
```

## `DCC_REST_INVITE_QUERY_INIT`

Public macro `DCC_REST_INVITE_QUERY_INIT`; arguments follow the owner header contract.

```c
#define DCC_REST_INVITE_QUERY_INIT ...
```

## `DCC_REST_INVITE_QUERY_VERSION`

Public macro `DCC_REST_INVITE_QUERY_VERSION`; arguments follow the owner header contract.

```c
#define DCC_REST_INVITE_QUERY_VERSION ...
```

## `DCC_REST_INVITE_TARGET_USERS_UPLOAD_INIT`

Public macro `DCC_REST_INVITE_TARGET_USERS_UPLOAD_INIT`; arguments follow the owner header contract.

```c
#define DCC_REST_INVITE_TARGET_USERS_UPLOAD_INIT ...
```

## `DCC_REST_INVITE_TARGET_USERS_UPLOAD_VERSION`

Public macro `DCC_REST_INVITE_TARGET_USERS_UPLOAD_VERSION`; arguments follow the owner header contract.

```c
#define DCC_REST_INVITE_TARGET_USERS_UPLOAD_VERSION ...
```

## `dcc_invite_params_init`

Public DCC API declaration for `dcc_invite_params_init`.

```c
static inline void dcc_invite_params_init(dcc_invite_params_t *value) { if (value != NULL) *value = DCC_INVITE_RECORD_INIT_VALUE(dcc_invite_params_t, DCC_INVITE_PARAMS_INIT); }
```

## `dcc_rest_invite_query_init`

Public DCC API declaration for `dcc_rest_invite_query_init`.

```c
static inline void dcc_rest_invite_query_init(dcc_rest_invite_query_t *value) { if (value != NULL) *value = DCC_INVITE_RECORD_INIT_VALUE(dcc_rest_invite_query_t, DCC_REST_INVITE_QUERY_INIT); }
```

## `dcc_rest_invite_target_users_upload_init`

Public DCC API declaration for `dcc_rest_invite_target_users_upload_init`.

```c
static inline void dcc_rest_invite_target_users_upload_init( dcc_rest_invite_target_users_upload_t *value ) { if (value != NULL) *value = DCC_INVITE_RECORD_INIT_VALUE(dcc_rest_invite_target_users_upload_t, DCC_REST_INVITE_TARGET_USERS_UPLOAD_INIT); } #ifdef __cplusplus }
```
