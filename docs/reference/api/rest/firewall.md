<!-- Generated by tools/generate_api_reference.py; do not edit. -->

# `<dcc/rest/firewall.h>`

Source: [`include/dcc/rest/firewall.h`](https://github.com/Feralthedogg/DCC/blob/v2.0.1/include/dcc/rest/firewall.h)

## `DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_HARD_LIMIT`

Public macro `DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_HARD_LIMIT`; arguments follow the owner header contract.

```c
#define DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_HARD_LIMIT ...
```

## `DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_SOFT_LIMIT`

Public macro `DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_SOFT_LIMIT`; arguments follow the owner header contract.

```c
#define DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_SOFT_LIMIT ...
```

## `DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_WINDOW_MS`

Public macro `DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_WINDOW_MS`; arguments follow the owner header contract.

```c
#define DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_WINDOW_MS ...
```

## `DCC_REST_FIREWALL_DEFAULT_SOFT_LIMIT_DELAY_MS`

Public macro `DCC_REST_FIREWALL_DEFAULT_SOFT_LIMIT_DELAY_MS`; arguments follow the owner header contract.

```c
#define DCC_REST_FIREWALL_DEFAULT_SOFT_LIMIT_DELAY_MS ...
```

## `dcc_rest_firewall_attach`

Public DCC API declaration for `dcc_rest_firewall_attach`.

```c
DCC_API dcc_status_t dcc_rest_firewall_attach( dcc_client_t *client, const dcc_rest_firewall_options_t *options );
```

## `dcc_rest_firewall_check`

Public DCC API declaration for `dcc_rest_firewall_check`.

```c
DCC_API dcc_status_t dcc_rest_firewall_check( dcc_rest_firewall_t *firewall, const dcc_rest_firewall_request_t *request, dcc_rest_firewall_result_t *result );
```

## `dcc_rest_firewall_create`

Creates one standalone firewall owner and clears output on failure.

```c
DCC_API dcc_status_t dcc_rest_firewall_create( const dcc_rest_firewall_options_t *options, dcc_rest_firewall_t **out_firewall );
```

## `dcc_rest_firewall_decision_string`

Public DCC API declaration for `dcc_rest_firewall_decision_string`.

```c
DCC_API const char *dcc_rest_firewall_decision_string(dcc_rest_firewall_decision_t decision);
```

## `dcc_rest_firewall_destroy`

Consumes a standalone firewall owner; NULL is allowed.

```c
DCC_API void dcc_rest_firewall_destroy(dcc_rest_firewall_t *firewall);
```

## `dcc_rest_firewall_detach`

Public DCC API declaration for `dcc_rest_firewall_detach`.

```c
DCC_API void dcc_rest_firewall_detach(dcc_client_t *client);
```

## `dcc_rest_firewall_hard_limit_action_string`

Public DCC API declaration for `dcc_rest_firewall_hard_limit_action_string`.

```c
DCC_API const char *dcc_rest_firewall_hard_limit_action_string( dcc_rest_firewall_hard_limit_action_t action );
```

## `dcc_rest_firewall_options_init`

Initializes standalone firewall options in caller-compiled storage.

```c
static inline void dcc_rest_firewall_options_init( dcc_rest_firewall_options_t *options ) { if (options != NULL) { dcc_rest_firewall_options_t value = { sizeof(value), DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_SOFT_LIMIT, DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_HARD_LIMIT, DCC_REST_FIREWALL_DEFAULT_INVALID_REQUEST_WINDOW_MS, DCC_REST_FIREWALL_DEFAULT_SOFT_LIMIT_DELAY_MS, DCC_REST_FIREWALL_HARD_LIMIT_REJECT_NONCRITICAL, }; *options = value; } }
```

## `dcc_rest_firewall_options_validate`

Public DCC API declaration for `dcc_rest_firewall_options_validate`.

```c
DCC_API dcc_status_t dcc_rest_firewall_options_validate( const dcc_rest_firewall_options_t *options );
```

## `dcc_rest_firewall_reason_string`

Public DCC API declaration for `dcc_rest_firewall_reason_string`.

```c
DCC_API const char *dcc_rest_firewall_reason_string(dcc_rest_firewall_reason_t reason);
```

## `dcc_rest_firewall_record_response`

Public DCC API declaration for `dcc_rest_firewall_record_response`.

```c
DCC_API dcc_status_t dcc_rest_firewall_record_response( dcc_rest_firewall_t *firewall, uint16_t status, uint64_t now_ms );
```

## `dcc_rest_firewall_request_is_critical`

Public DCC API declaration for `dcc_rest_firewall_request_is_critical`.

```c
DCC_API uint8_t dcc_rest_firewall_request_is_critical( const char *method, const char *path );
```

## `dcc_rest_firewall_snapshot`

Public DCC API declaration for `dcc_rest_firewall_snapshot`.

```c
DCC_API dcc_status_t dcc_rest_firewall_snapshot( dcc_client_t *client, dcc_rest_firewall_snapshot_t *snapshot );
```

## `dcc_rest_firewall_snapshot_json`

Public DCC API declaration for `dcc_rest_firewall_snapshot_json`.

```c
DCC_API dcc_status_t dcc_rest_firewall_snapshot_json( const dcc_rest_firewall_snapshot_t *snapshot, char *buf, size_t len, size_t *out_len );
```

## `dcc_rest_firewall_state_snapshot`

Public DCC API declaration for `dcc_rest_firewall_state_snapshot`.

```c
DCC_API dcc_status_t dcc_rest_firewall_state_snapshot( dcc_rest_firewall_t *firewall, uint64_t now_ms, dcc_rest_firewall_snapshot_t *snapshot );
```

## `dcc_rest_firewall_status_is_invalid`

Public DCC API declaration for `dcc_rest_firewall_status_is_invalid`.

```c
DCC_API uint8_t dcc_rest_firewall_status_is_invalid(uint16_t status);
```
