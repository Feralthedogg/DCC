# DCC 2.0 Public API Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace DCC 1.x's overlapping Sugar/App/REST API with one canonical Bot API, a focused App API, observable asynchronous REST requests, and ABI-versioned builders for DCC 2.0.

**Architecture:** Build the DCC 2 contracts beside the current implementation, migrate internals and in-tree consumers, then remove the DCC 1 public surface. The REST migration uses a checked endpoint manifest so hundreds of declarations and definitions cannot drift. Every task starts with a failing focused test and leaves its target set green.

**Tech Stack:** C11, CMake 3.20+, LLAM 2.2.0, existing DCC platform synchronization, Python 3 audit scripts, CTest, ASan/UBSan.

## Global Constraints

- Keep C11 and LLAM 2.2.0.
- Set release and shared-library ABI to 2.0.0 and 2 only in the release task.
- Do not revert or rewrite the existing uncommitted JSON parser and benchmark changes.
- Keep Linux, macOS, Windows, and BSD package checks supported.
- Builder construction performs no allocation.
- Async submission copies or serializes all borrowed input before returning.
- Do not ship a DCC 1 compatibility header in the DCC 2 package.

---

### Task 1: Define the DCC 2 Surface Contract Harness

**Files:**
- Create: `tests/api_v2_surface_smoke.c`
- Create: `tools/audit_v2_surface.py`
- Create: `tools/api_v2_removed_names.txt`
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: `docs/superpowers/specs/2026-08-11-dcc-2.0-api-redesign-design.md`.
- Produces: the strict `dcc_api_v2_surface_smoke` fixture, a transition-safe
  `dcc_v2_surface_audit` CI gate, and the strict audit mode used by Task 14.

- [x] **Step 1: Write the failing canonical compile test**

```c
#include <dcc/bot.h>

DCC_SLASH_FN(v2_ping) {
    (void)user_data;
    return DCC_CTX_REPLY_TEXT(ctx, "pong");
}

static const dcc_listener_t listeners[] = {
    DCC_LISTEN_SLASH("ping", "Reply with pong", v2_ping),
};

int main(void) {
    return listeners[0].kind == DCC_LISTENER_SLASH ? 0 : 1;
}
```

- [x] **Step 2: Verify RED**

Run `cc -std=c11 -Iinclude -fsyntax-only tests/api_v2_surface_smoke.c`.

Expected: `<dcc/bot.h>`, `dcc_listener_t`, and canonical macros are missing.

- [x] **Step 3: Write the failing strict audit and transition mode**

`audit_v2_surface.py` must compile every public header alone as C11 and C++, enforce at most 300 `DCC_` macros from `<dcc/bot.h>`, reject undocumented `DCC_API` declarations, reject declaration fragments, and reject every line in `api_v2_removed_names.txt`. Seed the banned list with `DCC_ON_`, `DCC_ROUTE_`, `DCC_DECORATE_`, `DCC_RESPOND`, `DCC_SIMPLE_BOT`, `DCC_V2_`, `dcc_rest_future_t`, and App REST mirror prefixes.

Add an explicit `--transition` mode for Tasks 1-13. It must still compile the
currently installed headers standalone and validate its own configuration, but
it records the known legacy-name and missing-Bot-header debt instead of failing
the default build. Strict mode remains the default and must never silently fall
back to transition behavior.

The four pre-existing App declaration fragments are exact, path-scoped,
self-invalidating transition compile-failure allowances until Task 2 deletes
them; any other C/C++ failure is fatal. Macro accounting uses the compiler's
complete macro dump plus a verified sentinel. Required banned seeds and the
final App export allowlist are immutable in the audit. With tests enabled,
Python, the toolchain-aware C++ compiler, and a resolved LLAM include directory
are required so the gate cannot silently disappear.

- [x] **Step 4: Verify the audit is RED**

Run `python3 tools/audit_v2_surface.py --source . --compiler "$(command -v cc)"`.

Expected: missing Bot header, macro budget, aliases, App fragments, and undocumented declarations are reported.

- [x] **Step 5: Register the green transition gate and commit the contract harness**

Register `dcc_v2_surface_audit` under `DCC_BUILD_TESTS` with `--transition`.
Keep the canonical compile fixture in-tree but do not register its build target
until Task 13 creates `<dcc/bot.h>` and can leave that target green. Commit the
new test fixture, audit, banned list, and CMake registration with message
`test: define DCC 2 public API contract`.

Completed in `bc4ee43`, `2ed96fd`, and review-hardening commit `cd0c28c`.

---

### Task 2: Introduce Focused App Headers and the Canonical Listener Registry

**Files:**
- Create: `include/dcc/app/base.h`
- Create: `include/dcc/app/options.h`
- Create: `include/dcc/app/lifecycle.h`
- Create: `include/dcc/app/listeners.h`
- Create: `include/dcc/app/context.h`
- Create: `include/dcc/app/modules.h`
- Create: `include/dcc/app/store.h`
- Create: `include/dcc/app/legacy.h` (transition-only; removed in Task 14)
- Create: `src/app/app_listener.c`
- Modify: `include/dcc/app.h`
- Modify: `src/internal/app/dcc_app_internal.h`
- Modify: `src/app/app_routes.c`
- Modify: `src/app/app_events.c`
- Modify: `src/app/app_scheduler.c`
- Modify: `src/app/app_definition.c`
- Create: `tests/app_v2_listener_smoke.c`
- Modify: `tests/app_smoke.c`
- Modify: `tools/audit_v2_surface.py`
- Modify: `CMakeLists.txt`
- Remove: `include/dcc/app/core_decls.h`
- Remove: `include/dcc/app/context_decls.h`
- Remove: `include/dcc/app/rest_decls_a.h`
- Remove: `include/dcc/app/rest_decls_b.h`

**Interfaces:**
- Consumes: current opaque App objects and internal route/event/task tables.
- Produces: standalone focused headers, `dcc_listener_t`, `dcc_app_listen`,
  `dcc_app_unlisten`, and status-returning canonical callbacks while legacy
  entry points remain isolated in one explicitly temporary header.

- [x] **Step 1: Add a failing handler-propagation case**

Register a canonical listener whose handler returns `DCC_ERR_STATE`, dispatch
its route through the App smoke intercept, and assert the temporary App error
adapter is called exactly once with `DCC_ERR_STATE`. Add invalid size, version,
kind/handler mismatch, unlisten, and cleanup-once cases in the same focused
target.

- [x] **Step 2: Verify RED**

Configure `build-v2-api` with tests, examples, tools, and sibling `../LLAM`;
build `dcc_app_v2_listener_smoke` and `dcc_app_smoke`. Expected: canonical
listener declarations are absent or the returned status is discarded. The Bot
compile target remains intentionally unregistered until Task 13.

- [x] **Step 3: Split canonical and transition-only callback types**

```c
typedef dcc_status_t (*dcc_app_handler_fn)(dcc_ctx_t *, void *);
typedef dcc_status_t (*dcc_app_typed_handler_fn)(dcc_ctx_t *, void *, void *);
typedef dcc_status_t (*dcc_app_event_fn)(dcc_app_t *, const dcc_event_t *, void *);
typedef dcc_status_t (*dcc_app_ready_fn)(dcc_app_t *, const dcc_ready_event_t *, void *);
typedef dcc_status_t (*dcc_app_message_fn)(dcc_app_t *, const dcc_message_t *, const dcc_event_t *, void *);
typedef dcc_status_t (*dcc_app_message_command_fn)(dcc_app_t *, const dcc_message_t *, const char *, const dcc_event_t *, void *);
typedef dcc_status_t (*dcc_app_task_fn)(dcc_app_t *, void *);
```

Cleanup callbacks remain `void`. Rename the old void callback types internally
and in `legacy.h` to `dcc_app_legacy_*_fn`; legacy registration functions use
only those names. This lets unmodified 1.x examples stay buildable during the
migration without weakening the canonical types.

`dcc_listener_t` has this fixed shape:

- `size`, `version`, and `dcc_listener_kind_t kind` first;
- a named handler union covering plain, typed, event, ready, message,
  message-command, and task callbacks;
- `user_data` plus an optional cleanup callback;
- one nested route policy;
- optional typed `args_size`, binding kind/list, validators, and validation
  response policy;
- a named target union for route identity, event identity/once, message-command
  identity, or schedule identity.

Kinds cover slash, subcommand, autocomplete, user/message context menu,
button/select/modal exact and prefix routes, generic event, ready,
message create/update/delete, message command, and scheduled task. Define
`DCC_LISTENER_VERSION` as `1U`; every nested evolvable value also starts with
`size` and `version`.

- [x] **Step 4: Implement the single registration path and removal ownership**

Implement `dcc_app_listen(app, listener, out_id)` and `dcc_app_unlisten(app, id)`.
Validate version, covered size, kind-specific fields, exactly one compatible
handler shape, policy, bindings, and validators before adapting to existing
internal tables. Track owned adapter state in the App so unlisten prevents
future dispatch and runs cleanup exactly once, including schedules and client
event listeners.

- [x] **Step 5: Propagate canonical statuses without changing legacy semantics**

Route, typed route, event, ready, message, and task adapters capture a returned
status and invoke the temporary error adapter once on non-OK. Legacy callbacks
remain void and keep their current behavior until Task 14 removes them.

- [x] **Step 6: Remove the declaration fragments**

Move canonical declarations into the seven standalone focused headers. Move the
remaining 1.x declarations and App REST mirrors into `legacy.h`, which includes
its own dependencies and compiles standalone. `<dcc/app.h>` aggregates the
seven focused headers plus this one clearly marked transition header. Delete
all four fragment paths so the Task 1 compile-debt allowance becomes stale and
must be removed from the audit in this task. Update Task 1's immutable App
allowlist to the exact canonical functions now declared by the seven headers;
never allow a broad verb prefix such as `dcc_app_create_*`.

- [x] **Step 7: Verify and commit**

Run focused App, canonical-listener, transition surface, and public-header
tests. All previous enabled tests remain green. Commit with message
`feat: add focused status-returning App API`.

Completed and review-approved through `606ff7b`. Final evidence includes full
enabled CTest 174/174, ASan/UBSan, TSan, MinGW C11, standalone C11/C++17
headers, exact registration transactions, callback-safe nonblocking stop, and
cleanup-safe retryable destruction.

---

### Task 3: Make Errors Structured and REST Failures Observable

**Files:**
- Create: `include/dcc/error_details.h`
- Create: `include/dcc/rest/result.h`
- Create: `src/internal/rest/dcc_rest_error_observer_internal.h`
- Create: `src/rest/rest_result.c`
- Create: `src/rest/rest_error_observer.c`
- Create: `tests/app_v2_error_smoke.c`
- Modify: `include/dcc/client.h`
- Modify: `include/dcc/rest/base.h`
- Modify: `include/dcc/rest.h`
- Modify: `include/dcc/app/base.h`
- Modify: `include/dcc/app/legacy.h` and transition Sugar/App-definition users
- Modify: `src/internal/client/dcc_client_rest_state_fields_internal.h`
- Modify: `src/internal/app/dcc_app_internal.h`
- Modify: `src/internal/rest/dcc_rest_request_raw_internal.h`
- Modify: `src/app/app_error.c`
- Modify: `src/app/app_listener.c`
- Modify: `src/app/app_routes.c`
- Modify: `src/app/app_definition.c`
- Modify: `src/rest/rest_request.c`
- Modify: `src/rest/rest_async_worker.c`
- Modify: `src/rest/rest_async_submit.c`
- Modify: `src/rest/rest_async_cancel_pending.c`
- Modify: async shutdown/drain delivery helpers as required
- Modify: package-consumer and Sugar status fixtures that use the old observer
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: status-returning dispatch from Task 2 and current HTTP responses.
- Produces: `dcc_error_t`, `dcc_rest_result_t`, result helpers, separate
  client/App error observers, and one final-completion reporting path shared by
  synchronous, asynchronous, intercepted, and canceled REST work.

- [x] **Step 1: Write failing value and ownership tests**

For transport success plus HTTP 400, assert `dcc_rest_result_ok()` is false and
`dcc_rest_result_status()` is `DCC_ERR_DISCORD`. A transport failure maps to
its transport status and a 2xx response is OK. Exercise null, invalid size and
version, and output-invalidation cases.

Use this public ownership contract:

```c
dcc_status_t dcc_rest_result_clone(
    const dcc_rest_result_t *source,
    dcc_rest_result_t **out
);
void dcc_rest_result_free(dcc_rest_result_t *result);
```

Clone body and Discord-message strings by exact lengths, including an embedded
NUL in the body, mutate/free the original buffers, and assert the clone remains
unchanged. Failed clone always leaves `*out == NULL`.

- [x] **Step 2: Write the final REST completion matrix RED tests**

Use the local HTTP server and REST interceptor to cover all of these cases:

- sync HTTP 400 without a request callback;
- sync HTTP 500 with a request callback, proving callback and observer each run
  once;
- transport failure;
- async transient 429 followed by 2xx, proving no transient observation;
- async terminal 429 after retry exhaustion, observed exactly once;
- pending cancellation, active cancellation, and shutdown drain, even when the
  request callback is null;
- intercepted non-2xx and intercepted transport failure;
- synchronous validation/queue-admission rejection, which is not a completion
  and therefore must not notify the observer.

For `{"code":50035,"message":"Invalid Form Body"}`, assert origin, mapped
status, HTTP 400, code 50035, decoded message, exact body length, and borrowed
operation/path. Malformed, truncated, non-object, and non-NUL-terminated JSON
must stay bounded and report the HTTP failure without inventing code/message.

- [x] **Step 3: Write App handler/default-policy RED tests**

Return `DCC_ERR_STATE` from route, event, and real scheduled-task listeners and
assert one size/versioned `DCC_ERROR_HANDLER` observation while the relevant
context/operation view is still alive. A route failure before any response must
queue exactly one fixed generic ephemeral response; a handler that already
responded must not trigger a second response. The generic text must never
contain the handler message, Discord response body, token, or raw transport
details.

Install a public client observer and the App's private client sink together and
prove one REST failure reaches both without either registration overwriting the
other. Observer callbacks must be able to clear or replace themselves without
deadlock.

- [x] **Step 4: Verify RED**

Build and run `dcc_app_v2_error_smoke`. Expected: structured result/error
symbols are missing and the temporary context-shaped App observer cannot satisfy
the owner-typed contract.

- [x] **Step 5: Implement versioned result and error views**

Define public `DCC_REST_RESULT_VERSION` and `DCC_ERROR_VERSION` constants.
Both values start with `size` and `version`; every helper validates the covered
prefix before reading. Implement `dcc_rest_result_ok`,
`dcc_rest_result_status`, clone, and free. Parse Discord code/message with the
existing bounded JSON parser and the supplied `body_len`; never use `strlen` on
response bytes.

Use owner-typed callbacks rather than a `void *owner` abstraction:

```c
typedef void (*dcc_client_error_fn)(
    dcc_client_t *,
    const dcc_error_t *,
    void *
);
typedef void (*dcc_app_error_fn)(
    dcc_app_t *,
    const dcc_error_t *,
    void *
);
```

All error fields are borrowed only for the callback. `dcc_client_on_error` and
`dcc_app_on_error` accept `(NULL, NULL)` to clear; a null callback with non-null
user data is invalid. Document that replacing an observer does not revoke a
callback already copied for delivery, so old user data remains alive until any
in-flight callback returns.

- [x] **Step 6: Separate public and App-internal observer slots**

Store the public client observer and the App-owned internal sink independently.
Protect registration with a dedicated lock, copy callback/user-data pairs under
that lock, and invoke all user callbacks after unlocking. App observer delivery
uses the App listener lock with the same copy-then-call rule. Reentrant clear or
replacement must not deadlock, and setting the App sink must never clobber the
client user's observer.

- [x] **Step 7: Report only final REST failures exactly once**

Do not report inside a raw attempt used by the async retry loop. Synchronous raw
REST observes after its final retry; the async worker observes only after the
retry/requeue decision reaches a terminal completion. Wrap interceptor
callbacks, pending/active cancellation, and shutdown drain through the same
completion helper. Supplying a legacy per-request callback never suppresses the
observer. No callback is required for observation.

The observer maps transport failures directly and every terminal non-2xx to
`DCC_ERR_DISCORD`. Current legacy `dcc_rest_response_t::error` behavior remains
unchanged until Task 4 replaces that callback surface. Never hold the REST
queue/rate-limit/observer lock while invoking a request callback, public
observer, App sink, or logger.

- [x] **Step 8: Replace the temporary App observer safely**

Replace the temporary context/status/message callback with the structured
App-owned observer and migrate transition App-definition/Sugar fixtures in the
same commit. Handler delivery occurs before route context release. The safe
default end-user policy is independent from telemetry observers: if a handler
fails before a response starts, send one fixed generic ephemeral error; if a
response already started, send nothing. Route/event/task failures each notify
the App observer once. REST errors forwarded through the private client sink do
not create a second client observation.

- [x] **Step 9: Verify, sanitize, review, and commit**

Run `dcc_app_v2_error_smoke`, `dcc_http_smoke`, App listener/concurrency tests,
package consumers, transition surface/public/header audits, and the full enabled
CTest suite. Run focused ASan/UBSan and TSan for observer re-registration,
async completion/cancel, and App destruction. Commit with message
`feat: add structured observable errors`.

---

### Task 4: Unify Async REST Requests

**Files:**
- Create: `include/dcc/rest/request.h`
- Create: `src/internal/rest/dcc_rest_request_handle_internal.h`
- Create: `src/rest/rest_request_handle.c`
- Create: `src/rest/rest_submit.c`
- Create: `tests/rest_v2_request_smoke.c`
- Modify: `include/dcc/rest.h`
- Modify: `src/internal/rest/dcc_rest_async_request_internal.h`
- Modify: `src/internal/rest/dcc_rest_error_observer_internal.h`
- Modify: `src/rest/rest_async_request.c`
- Modify: `src/rest/rest_async_request_new.c`
- Modify: `src/rest/rest_async_worker.c`
- Modify: `src/rest/rest_async_cancel.c`
- Modify: `src/rest/rest_async_cancel_pending.c`
- Modify: `src/rest/rest_error_observer.c`
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: the existing priority/rate-limit async queue and Task 3 results.
- Produces: version-1 `dcc_rest_call_options_t` and
  `dcc_rest_request_desc_t`, opaque `dcc_rest_request_t`, `dcc_rest_submit`,
  wait/cancel/completed/destroy, and one finalization path shared with the
  transition async/future surface.

- [x] **Step 1: Write delayed-server RED tests**

Delay the server or synchronous REST interceptor by 250 ms, assert
`dcc_rest_submit()` returns in under 100 ms, mutate every submitted path,
content-type, and binary-body buffer immediately, then assert the server sees
the original exact bytes. Wait on the request and assert HTTP 200. Include an
embedded NUL body.

Add timeout-then-wait, two concurrent waiters, pending and active cancellation,
destroy-while-active, destroy-from-callback, callback-once, clone-retention,
handle-survives-client-destroy, callback-only auto-release, and no-handle
fire-and-observe cases. A successful wait must happen only after callback and
observer return. Same-request wait from its callback returns `DCC_ERR_STATE`.

Cover null and invalid size/version/method/priority/path/body and
callback/user-data combinations. Every rejection clears the output and invokes
neither callback nor observer. Inject worker-spawn admission failure and cover
an interceptor that returns success without a response; the former is a silent
rejection and the latter is one accepted `DCC_ERR_RUNTIME` completion.

- [x] **Step 2: Verify RED**

Build and run `dcc_rest_v2_request_smoke`. Expected: request handle APIs are missing.

- [x] **Step 3: Define the canonical public request contract**

Publish `DCC_REST_CALL_OPTIONS_VERSION`,
`DCC_REST_REQUEST_DESC_VERSION`, fully documented initializer macros and
functions, `dcc_rest_result_fn`, and the opaque request operations in the
standalone C11/C++ header. Null call options mean normal priority/no callback;
zero wait timeout means infinite. Wait returns `DCC_OK` for any completed
operation and exposes the outcome through its borrowed result. Timeout clears
the result output without invalidating the handle. Cancel is idempotent;
completed on null is false and destroy on null is a no-op.

- [x] **Step 4: Implement reference-counted request state and waits**

Use one caller reference when an output handle is requested and one runtime reference while queued/active. Completion stores an owned result, signals waiters, invokes the callback once, reports errors, and drops the runtime reference. Destroy requests cancellation and drops the caller reference without blocking.

Terminal claiming must be atomic with respect to cancellation. Build the owned
result before calling user code, invoke the result callback and existing Task 3
observers without internal locks, then publish completion and wake every waiter.
Keep the runtime reference through all delivery so destroy-from-callback is
safe. Detect same-request recursive waits instead of deadlocking. The result
body is an exact owned byte span; decoded Discord metadata is owned for the
handle lifetime.

- [x] **Step 5: Implement raw submission and payload ownership**

`dcc_rest_submit()` validates the versioned descriptor and nested call options,
resolves the enum method, and copies path, content type, and exact body bytes
before queue admission. The callback/user-data pair is the only intentionally
retained borrowed input. On every local or injected admission failure, free all
copies, clear `*out_request`, and produce no terminal delivery. On success,
publish the caller reference before the worker may invoke its callback.

Always enqueue the new API, including under the synchronous test interceptor;
never execute network or interceptor work on the submitting thread. A success
from an interceptor that did not synchronously supply one valid response is a
terminal runtime failure, not a disappearing completion.

- [x] **Step 6: Implement exact per-request cancellation**

Under the existing REST lock, distinguish queued, active, terminalizing, and
completed requests. Exact cancellation publishes cancellation but keeps a
selected pending/retry-queued request owned by the queue; it must never invoke
terminal user code on the public cancel/destroy stack. When work can be
admitted, run the canceled entry through the existing runtime worker, bypassing
route, rate-limit, and retry-delay eligibility while preserving priority and
concurrency. Track whether each job actually claimed its route so a canceled
same-route worker cannot release another request's exclusion. A spawn failure
restores the exact queue position, and stopping leaves the canceled entry
queue-owned for bulk teardown. Atomically mark active cancellation and close
its exchanged fd after unlocking. Cancellation is nonblocking and idempotent;
the first terminal claimant wins. Shutdown and bulk pending cancellation may
deliver on the teardown caller, use the same handle finalizer, and release the
runtime reference exactly once.

- [x] **Step 7: Adapt the worker without changing scheduling semantics**

Connect exact body length/content type, owned result construction, callback,
observer, and runtime-reference release to the existing worker. Preserve
priority, per-route exclusion, rate-limit retries, firewall behavior, retry
wakeups, admission rollback, and shutdown drain. A transient 429 followed by
2xx has one successful completion and no transient observation; terminal 429,
transport failure, cancellation, and shutdown each complete and observe once.

Keep legacy async/future functions buildable as transition adapters until Task
14, but do not expose the new endpoint path through `dcc_rest_future_t` and do
not change its public ABI in this task.

- [x] **Step 8: Verify public, lifecycle, and platform contracts**

Run `dcc_rest_v2_request_smoke`, `dcc_http_smoke`, Task 3 error/App lifecycle
targets, package consumers, and transition surface/header audits. Compile the
new header alone as C11 and C++, compile the Windows include-order fixture with
MinGW, and run focused ASan/UBSan and TSan cases for concurrent wait/cancel,
destroy while active, callback reentry, admission rollback, and client teardown.

- [x] **Step 9: Commit**

Commit the request API, internals, tests, and CMake changes with message `feat: unify asynchronous REST requests`.

---

### Task 5: Version Core Caller-Owned Builders

**Files:**
- Modify: `include/dcc/message.h`
- Modify: `include/dcc/embed.h`
- Modify: `include/dcc/modal.h`
- Modify: `include/dcc/autocomplete.h`
- Modify: `include/dcc/application_command.h`
- Modify: matching serializers and builders under `src/`
- Modify: command-registry builder copy/cleanup paths under `src/command_registry/`
- Create: `tests/builder_v2_abi_smoke.c`
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: current caller-owned builder layouts.
- Produces: allocation-free, size/versioned message, embed, modal,
  autocomplete, and command builders for all later endpoint work.

- [ ] **Step 1: Write historical-prefix RED tests**

For each builder family, initialize a fixture whose `size` covers only a
hand-checked historical prefix, place poison bytes after that prefix, serialize
it, and assert only covered fields are read. Reject zero size, unsupported
version, unknown presence bits, and presence bits naming uncovered fields.
Exercise setters against covered and uncovered prefixes and assert a rejected
setter leaves every byte and surrounding canary unchanged.

Use packed, suitably aligned historical-layout arrays with two distinct
elements for embed builders, autocomplete choices, application commands, and
recursive command options. Prove iteration follows the declared element stride
rather than the library's current `sizeof`. Cover overflow, inconsistent
element sizes, and malformed nested elements.

- [ ] **Step 2: Verify RED**

Build and run `dcc_builder_v2_abi_smoke`. Expected: current layouts have no
version contract and serializers read the full struct.

- [ ] **Step 3: Add exact layout and presence contracts**

The evolvable public inputs are message, poll, allowed-mentions, embed, modal,
autocomplete, autocomplete-choice, application-command, and application-
command-option builders. Every one starts with `size_t size`,
`uint32_t version`, then `uint64_t present`. Define a `*_VERSION` constant,
public presence-bit constants, a constant initializer, and a null-safe init
function per type. Construction and initialization allocate nothing. Existing
setter functions set or clear the matching presence bit while preserving their
documented null/empty semantics; remove redundant `has_*` bytes.

Keep raw JSON and legacy component fields only as transition-gated members
until Task 14. Fixed embedded leaf records such as embed fields and poll answers
do not grow within ABI major 2; component builders are owned by Tasks 10 and 14.

- [ ] **Step 4: Gate every serializer read by covered size**

Centralize field-coverage checks per family. Required fields missing from a
historical prefix return a named validation error; optional uncovered fields
behave as absent. Never `memcpy` beyond caller-declared size.

For every array of versioned records, use the first element's validated `size`
as a checked byte stride and require the same size/version for each element.
Normalize long-lived command-registry copies into initialized current-layout
storage and deep-copy only covered borrowed fields. Audit every serializer,
validator, setter, registry copy, and recursive cleanup so no direct full-layout
read or raw struct assignment bypasses the helpers.

- [ ] **Step 5: Verify and commit**

Run builder ABI, message/component, command registry, HTTP, and sanitizer-focused
tests. Commit with message `feat: version public message builders`.

---

### Task 6: Create the Endpoint Manifest and Convert Messages, Interactions, and Webhooks

**Files:**
- Create: `tools/rest_v2_endpoints.json`
- Create: `tools/audit_rest_v2_endpoints.py`
- Modify: `include/dcc/rest/messages/`, `include/dcc/rest/interactions/`, and
  `include/dcc/rest/webhooks/`
- Modify: matching `src/rest/rest_message_*`, `rest_interaction_*`, and
  `rest_webhook_*` units
- Modify: `tests/support/http_smoke_rest_*.c`
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: Task 4 request handles and Task 5 versioned message builders.
- Produces: one async typed submit function per message, interaction, and
  webhook endpoint plus a checked migration manifest.

- [ ] **Step 1: Define the complete manifest and progress audit**

Record every Discord endpoint's canonical name, domain, method, route
formatter, typed input, implementation file, and multipart flag. Strict mode
rejects duplicates, missing declarations/definitions, `dcc_rest_cb`,
`user_data`, raw JSON bodies, and overload suffixes. An explicit progress mode
allows only domains assigned to later Tasks 7-10 and fails on stale allowances.

- [ ] **Step 2: Prove audit RED and test one endpoint end-to-end**

Verify current overload groups fail strict audit. For create-message, use a
delayed local server and assert typed submission returns before completion,
owns serialized data, and exposes the final result through its handle.

- [ ] **Step 3: Convert message and interaction endpoints**

Each canonical function validates typed input, formats its route, serializes
once, and submits with optional call options/request output. Delete raw JSON,
text, builder, callback, sync, async, and future overloads for converted
endpoints and migrate their in-tree callers in the same commit.

- [ ] **Step 4: Convert webhook and multipart endpoints**

Copy payload and file bytes before returning. Preserve thread/query options in
typed params rather than function suffixes. Test zero files, multiple files,
cancellation, and callback/handle coexistence.

- [ ] **Step 5: Verify and commit**

Run the progress endpoint audit, HTTP smoke, interaction flow smoke, official
surface smoke, and transition surface audit. Commit with message
`feat: migrate message REST endpoints to requests`.

---

### Task 7: Convert Channel, Thread, and Invite Endpoints

**Files:**
- Modify: `include/dcc/rest/channels/`, `include/dcc/rest/threads/`, and
  `include/dcc/rest/invites/`
- Modify: matching channel/thread/invite units under `src/rest/`
- Modify: endpoint manifest and HTTP smoke support files

**Interfaces:**
- Consumes: Task 6 manifest and Task 4 request API.
- Produces: one canonical typed request function for channels, permissions,
  pins, reactions scoped to channels, threads, archives, members, and invites.

- [ ] **Step 1: Add domain RED fixtures**

Cover a GET without body, a typed PATCH, a paginated archive request, a
permission overwrite, and an invite query. Assert route/query encoding from
literal expected paths.

- [ ] **Step 2: Convert leaf and paginated operations**

Collapse overloads, move optional query values into size/versioned params, and
migrate all in-tree callers. Preserve audit-log reasons through call options or
typed endpoint input.

- [ ] **Step 3: Convert thread and multipart-adjacent operations**

Ensure owned strings/arrays survive caller mutation after submit and that
archive pagination/result handling is asynchronous.

- [ ] **Step 4: Verify and commit**

Require the endpoint progress audit to report these domains complete; run HTTP,
official surface, and header audits. Commit with message
`feat: migrate channel REST endpoints to requests`.

---

### Task 8: Convert Guild, Member, Role, Ban, and Prune Endpoints

**Files:**
- Modify: `include/dcc/rest/guilds/`, `include/dcc/rest/guild_members/`, and
  `include/dcc/rest/roles/`
- Modify: matching guild/member/role units under `src/rest/`
- Modify: endpoint manifest and HTTP smoke support files

**Interfaces:**
- Consumes: Task 6 manifest and Task 4 request API.
- Produces: canonical typed requests for guild lifecycle/configuration,
  members, roles, bans, integrations, prune, widget/welcome, and voice regions.

- [ ] **Step 1: Add literal-route and ownership RED cases**

Cover guild fetch, member pagination, role positions, ban duration/delete, and
prune options. Mutate every borrowed array/string after submit and assert the
server receives the original data.

- [ ] **Step 2: Convert read and write endpoints by resource**

Use typed params/builders only; remove overload suffixes and callback arguments.
Keep one operation name per Discord endpoint and migrate internal consumers.

- [ ] **Step 3: Verify and commit**

Run the endpoint progress audit plus HTTP, command registry, official surface,
and header tests. Commit with message
`feat: migrate guild REST endpoints to requests`.

---

### Task 9: Convert Application Commands, Auto Moderation, and Guild Resource Endpoints

**Files:**
- Modify: application-command, auto-moderation, emoji/sticker, scheduled-event,
  stage, onboarding, and template headers under `include/dcc/rest/`
- Modify: matching endpoint units under `src/rest/`
- Modify: `src/command_registry/` consumers
- Modify: endpoint manifest and HTTP smoke support files

**Interfaces:**
- Consumes: versioned command builders and request handles.
- Produces: canonical async command synchronization and typed guild resource
  operations without captured synchronous callbacks.

- [ ] **Step 1: Add command-sync and resource RED cases**

Test bulk overwrite, paginated scheduled-event users, multipart sticker upload,
auto-moderation actions, onboarding, and template sync using literal routes and
owned inputs.

- [ ] **Step 2: Convert command and moderation endpoints**

Rewrite command registry fetch/apply as request-handle state machines; do not
block a runtime worker waiting on another runtime request.

- [ ] **Step 3: Convert remaining guild resources**

Convert emoji/sticker, scheduled event, stage, onboarding, and template groups;
copy multipart bytes before queue admission.

- [ ] **Step 4: Verify and commit**

Run endpoint progress audit, command registry tests, HTTP smoke, and transition
surface audit. Commit with message
`feat: migrate command REST endpoints to requests`.

---

### Task 10: Finish REST Domains, Migrate Composite Consumers, and Remove App Mirrors

**Files:**
- Modify: remaining user, voice-state, entitlement, SKU, role-connection,
  gateway, DM/group-DM, and miscellaneous REST headers/units
- Modify: `src/app/`, `src/command_registry/`, `src/managed_message.c`, and
  `src/interactions/` consumers
- Remove: `src/app/app_rest_shortcuts.c`
- Remove: `src/app/app_rest_shortcuts_guilds.c`
- Remove: `src/app/app_rest_shortcuts_resources.c`
- Remove: `src/app/app_rest_shortcuts_webhooks.c`
- Modify: `include/dcc/app/legacy.h`
- Modify: endpoint manifest, endpoint audit, and `CMakeLists.txt`

**Interfaces:**
- Consumes: converted domain APIs from Tasks 6-9.
- Produces: a strict-green endpoint manifest, no App REST mirror, and async
  state machines for every composite operation.

- [ ] **Step 1: Convert every remaining manifest entry**

Progress mode must become stale and fail once the last domain is complete;
remove it and make strict mode the registered CTest gate.

- [ ] **Step 2: Rewrite composite consumers**

Direct-message creation, managed-message publish, command sync, inferred-guild
operations, and other multi-request flows use explicit owned state machines.
No runtime worker performs a blocking request wait.

- [ ] **Step 3: Remove App REST declarations and units**

Internal App code calls `dcc_rest_*` through `dcc_app_client(app)`. Keep only
App lifecycle/state/module/store/listener APIs and context-local interaction
actions. The endpoint audit must prove no App REST mirror declaration remains;
Task 1 may still report unrelated legacy alias debt until Task 14.

- [ ] **Step 4: Verify and commit**

Run strict endpoint audit, HTTP/official surface/App/managed-message/command
tests, and transition v2 audit. Commit with message
`feat: complete request-based REST API`.

---

### Task 11: Queue Interaction Actions in Order

**Files:**
- Create: `src/internal/app/dcc_app_interaction_queue_internal.h`
- Create: `src/app/app_interaction_queue.c`
- Create: `src/app/app_interaction_queue_worker.c`
- Create: `tests/app_v2_interaction_queue_smoke.c`
- Modify: `include/dcc/app/context.h`
- Modify: `include/dcc/interaction_flow.h`
- Modify: `src/app/app_context.c`
- Modify: `src/app/app_auto_defer.c`
- Modify: `src/interaction_flow/interaction_flow_messages.c`
- Modify: `src/interaction_flow/interaction_flow_defer.c`
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: async typed interaction endpoints from Tasks 6 and 10.
- Produces: owned per-interaction queues and nonblocking context actions.

- [ ] **Step 1: Write ordered-flow RED tests**

Queue two replies before the delayed first response. Assert the server receives an initial callback then a followup. Add defer/reply, auto-defer race, and first-operation HTTP failure cases; dependent actions must fail locally in order.

- [ ] **Step 2: Verify RED**

Build and run `dcc_app_v2_interaction_queue_smoke`. Expected: current synchronous flow cannot satisfy delayed ordered submission.

- [ ] **Step 3: Implement owned interaction state and sequential dispatch**

Copy IDs, token, response visibility, and serialized actions. Dispatch only the head. On success update state and submit the next; on required failure mark dependents `DCC_ERR_STATE` without network calls.

- [ ] **Step 4: Integrate context and auto-defer**

Context calls append and return queue-admission status. Atomically claim the initial slot. Add `INITIAL_QUEUED` and `DEFERRED_QUEUED` states.

- [ ] **Step 5: Verify and commit**

Run interaction queue, App, and interaction-flow smoke tests. Commit with message `feat: queue interaction responses asynchronously`.

---

### Task 12: Implement Tagged, Versioned Components v2

**Files:**
- Modify: `include/dcc/component_v2.h`
- Create: `src/component_v2/component_v2_value.c`
- Modify: matching component builders/serializers under `src/component_v2/`
- Create: `tests/component_v2_abi_smoke.c`
- Modify: `tests/component_v2_smoke.c`
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: Task 5 builder ABI rules.
- Produces: a size/versioned tagged union for Components v2 with no invalid
  cross-kind field combinations.

- [ ] **Step 1: Write ABI and tag RED cases**

Test a historical prefix size, every supported component kind, required-field
validation, and button/select/layout/media/text-input field mixing. Invalid
mixes return a named field error and never serialize ambiguous JSON.

- [ ] **Step 2: Verify RED**

Build component ABI and existing component smoke targets. Expected: current
flat builder accepts fields that are invalid for its active type.

- [ ] **Step 3: Implement the tagged value**

Start with `size`, `version`, type, and presence bits. Use named union members
for button, select, layout, media, text input, and modal data. Constructors set
the active kind and required bits without allocation.

- [ ] **Step 4: Harden serializers and compatibility reads**

Gate reads by caller size, reject inactive union fields, and preserve valid
historical-prefix behavior. Test nested component arrays and maximum Discord
limits.

- [ ] **Step 5: Verify and commit**

Run component ABI/smoke, message builder, transition surface, and sanitizer
tests. Commit with message `feat: tag and version Components v2`.

---

### Task 13: Build the Canonical Bot API

**Files:**
- Create: `include/dcc/bot.h`
- Create: `include/dcc/bot/handlers.h`
- Create: `include/dcc/bot/listeners.h`
- Create: `include/dcc/bot/replies.h`
- Create: `include/dcc/bot/ui.h`
- Create: `include/dcc/bot/main.h`
- Modify: `tests/api_v2_surface_smoke.c`
- Create: `tests/bot_v2_surface_smoke.c`
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: canonical App listeners from Task 2, ordered actions from Task 11,
  and tagged UI values from Task 12.
- Produces: `<dcc/bot.h>`, status handler declarations, flat `DCC_LISTEN_*`,
  canonical context actions, four entrypoints, and `DCC_UI_*`.

- [ ] **Step 1: Expand the Bot RED fixture**

Compile one Bot-only app with simple and policy-configured typed listeners,
event/task listeners, replies, a Components v2 card, development entrypoint,
and production entrypoint. No Sugar or aggregate Core include is allowed.

- [ ] **Step 2: Implement focused Bot headers**

Expose only the nine handler declarations, simple/`*_WITH` flat listener
constructors, canonical context action families, `DCC_UI_*`, and the four
entrypoint macros. Prefer static inline constructors; macros exist only where C
declarations or variadic compound arrays require them.

- [ ] **Step 3: Register the canonical compile gate**

Register `dcc_api_v2_surface_smoke` now that it can stay green. Verify every Bot
macro has an ownership/behavior comment and the full preprocessed Bot surface
contains at most 300 `DCC_` macros using Task 1's complete macro dump.

- [ ] **Step 4: Verify and commit**

Run Bot/API/App/interaction/component tests and the transition surface audit.
Commit with message `feat: add canonical Bot API`.

---

### Task 14: Delete DCC 1 API and Migrate the Repository

**Files:**
- Create: `tools/dcc_migrate_v2.py`
- Create: `docs/migration-1-to-2.md`
- Modify: `include/dcc/dcc.h`
- Remove: `include/dcc/sugar.h`
- Remove: `include/dcc/sugar/`
- Remove: `include/dcc/app/legacy.h`
- Modify: App internals to delete legacy callback adapters and entry points
- Modify: `examples/*.c` and `examples/hot_reload/*.c`
- Modify: `tools/dcc_new_app_*.c`
- Modify or remove: DCC 1 Sugar tests and CMake targets
- Modify: `CMakeLists.txt`

**Interfaces:**
- Consumes: complete DCC 2 APIs.
- Produces: no DCC 1 public names in headers, examples, generators, tests, or install output.

- [ ] **Step 1: Run strict audit and verify RED**

Run `audit_v2_surface.py` without legacy allowance. Expected: only explicit legacy headers, examples, templates, and alias tests remain.

- [ ] **Step 2: Test and implement migration tool**

Fixture mappings are `<dcc/sugar.h>` to `<dcc/bot.h>`, `DCC_SIMPLE_BOT_MAIN` to `DCC_BOT_MAIN`, `DCC_RESPOND_TEXT` to `DCC_CTX_REPLY_TEXT`, and `DCC_V2_TEXT` to `DCC_UI_TEXT`. Decorator and combinatorial typed-route fixtures must produce file/line manual-conversion diagnostics rather than unsafe rewrites.

- [ ] **Step 3: Migrate examples, generators, and behavioral tests**

Every normal example uses only Bot headers, flat listeners, status handlers, and canonical UI. Move useful behavior assertions to Bot/App/REST tests; delete tests that only prove aliases expand.

- [ ] **Step 4: Delete legacy headers and implementation adapters**

Remove the Sugar tree, transition-only App header, void-callback registration
entry points, and their internal adapters using patch deletion. `<dcc/app.h>`
now aggregates exactly the seven focused headers. Update install audits,
source-package checks, and CMake test targets.

- [ ] **Step 5: Verify and commit**

Run strict surface audit, full build, and full CTest. Commit with message `feat!: remove DCC 1 public API`.

---

### Task 15: Publish the 2.0 Contract and Verify Release Quality

**Files:**
- Create: `tools/api_v2_symbols.txt`
- Create: `docs/reference/api/*.md`
- Modify: `CMakeLists.txt`
- Modify: `README.md`, `CHANGELOG.md`, and `mkdocs.yml`
- Modify: primary pages under `docs/`
- Modify: `tools/audit_public_api.py`
- Modify: `tools/audit_release_contract.py`

**Interfaces:**
- Consumes: final DCC 2 headers and exported symbols.
- Produces: DCC 2.0.0 package metadata, generated reference, and 2.x compatibility baseline.

- [ ] **Step 1: Write and verify failing release assertions**

Require project version 2.0.0, SOVERSION 2, LLAM floor 2.2.0, a DCC 2 symbol baseline, no Beta copy, and no primary `<dcc/sugar.h>` include. Run the release audit and confirm version/reference failures.

- [ ] **Step 2: Set 2.0.0 and reset compatibility baseline**

Change `project(dcc VERSION 2.0.0)`. Permit origin/main removals only because the current major exceeds the baseline major; all future 2.x checks compare against `api_v2_symbols.txt`.

- [ ] **Step 3: Generate symbol docs and rewrite primary guidance**

Generate checked per-header Markdown from comments. README and Quickstart show one Bot path. Ownership covers request handles and result cloning. Components docs use `DCC_UI_*`; Core docs use explicit component functions.

- [ ] **Step 4: Run normal and sanitizer matrices**

Run a full tests/examples/tools/benchmarks build and complete CTest in `build-v2-api`. Configure `build-v2-api-asan` with `DCC_ENABLE_SANITIZERS=ON`, build, and run complete CTest.

- [ ] **Step 5: Run release and package checks**

Run `tools/release_check.sh build-v2-release`, then build the package target. Require standalone headers, surface audit, endpoint manifest, symbol baseline, docs, package consumers, tests, sanitizers, and benchmarks to pass.

- [ ] **Step 6: Commit**

Commit version, docs, manifests, and release audits with message `release: prepare DCC 2.0.0 API`.

---

## Final Review Checklist

- [ ] `git diff --check` is clean and unrelated JSON changes are preserved.
- [ ] `<dcc/bot.h>` exposes at most 300 DCC macros.
- [ ] No banned DCC 1 public name remains.
- [ ] Every public header compiles standalone as C11 and C++.
- [ ] Every App handler returns `dcc_status_t`.
- [ ] Typed REST submission is nonblocking and returns an optional request handle.
- [ ] Every non-2xx reaches the structured observer.
- [ ] Interaction actions preserve order and auto-defer correctness.
- [ ] Every evolvable caller-owned builder is size/versioned.
- [ ] Examples, generators, docs snippets, CTest, ASan/UBSan, release checks, and package checks pass.
