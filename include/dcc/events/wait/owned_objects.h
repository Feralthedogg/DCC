#ifndef DCC_EVENTS_WAIT_OWNED_OBJECTS_H
#define DCC_EVENTS_WAIT_OWNED_OBJECTS_H

/* Compatibility aggregate for owned object event wait helpers. */

#include <dcc/events/wait/owned_objects/single.h>
#include <dcc/events/wait/owned_objects/any.h>

#endif
