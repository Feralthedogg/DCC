name: CI

permissions:
  contents: read

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  release-check:
    name: release-check (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
          - os: macos-latest

    steps:
      - name: Checkout DCC
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          path: DCC

      - name: Checkout LLAM
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          repository: ${{ github.repository_owner }}/LLAM
          ref: 7265b339a9364502e1e537a3aec072939e2bdcf9 # v2.2.1
          path: LLAM

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake libssl-dev liburing-dev libopus-dev ninja-build python3

      - name: Install macOS dependencies
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install cmake ninja openssl@3 opus
          echo "OPENSSL_ROOT_DIR=$(brew --prefix openssl@3)" >> "$GITHUB_ENV"
          echo "OPUS_ROOT=$(brew --prefix opus)" >> "$GITHUB_ENV"

      - name: Run release check
        working-directory: DCC
        env:
          DCC_CMAKE_GENERATOR: Ninja
          DCC_LLAM_USE_SUBDIRECTORY: ON
          DCC_BUNDLE_LLAM: OFF
          DCC_LLAM_ROOT: ${{ github.workspace }}/LLAM
          DCC_CTEST_TIMEOUT: 300
          # Hosted runners are not deterministic enough for process/network timing tests.
          # The complete suite remains covered by local release_check.sh runs.
          DCC_CTEST_EXCLUDE: ^(dcc_cluster_chaos_smoke|dcc_cluster_gateway_smoke|dcc_cluster_status_route_smoke|dcc_interaction_server_smoke|dcc_hot_reload_health_route_smoke|dcc_hot_reload_canary_integration_smoke|dcc_hot_reload_isolated_.*)$
          DCC_ASAN_CTEST_EXCLUDE: ^(dcc_cluster_chaos_smoke|dcc_cluster_gateway_smoke|dcc_cluster_status_route_smoke|dcc_interaction_server_smoke|dcc_hot_reload_health_route_smoke|dcc_hot_reload_canary_integration_smoke|dcc_hot_reload_isolated_.*)$
        run: tools/release_check.sh
